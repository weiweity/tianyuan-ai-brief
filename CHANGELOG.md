# Changelog

本文件记录治理工具与合同候选的版本变化，不代表客服产品版本、阶段签发或生产上线。既有发布历史保留在 Git 与对应阶段记录中。

## [5.32.2] - 2026-09-09

### Added

- 后端身份与内容闭环 DRAFT 候选合同（OpenAPI/存储/事务增量）及静态/隔离 PG15 行为检查；静态检查接入 `test:release`。

### Changed

- 候选合同保持未冻结、未 intake；不表示 HTTP/worker 已实现或 DEV-M2 已获准。
- 静态检查钉死 LoginExchange 成功/多余字段、resume 能力、SQL 原因词与 SET ROLE 行为脚本；PG 行为补 PKCE 错 verifier、跨角色 EXECUTE、质量幂等冲突与缺陷阻断。
- 静态门把 SQLSTATE 与 reason 成对钉死，并覆盖 worker 对 page/list 的 GRANT 拒绝；PG 行为区分阈值阻断与低于阈值的 revision_required。

### Scope

- 仅治理仓候选合同与检查接线，不改变冻结上游 OpenAPI 1.12.0 / schema 1.15，不授权真实运行。

## [5.32.1] - 2026-09-06

### Changed

- 按本次任务文件选择迭代校验，完整发布检查共用唯一执行顺序；私有工作区与真实页面排版保留独立且必跑的发布门禁。
- main 通过完整检查后复用同一份安全产物发布；PR 不部署，纯文档后继提交不会导致先前页面改动漏发。
- 统一工程校验入口与授权说明，保留业务签发、冻结合同和公开候选批准边界。

### Fixed

- 排版浏览器缺失时明确失败；补齐事件条件、连续提交和测试子进程失败退出回归。

### Scope

- 仅内部治理与交付流程，不改变客服业务状态、机器合同、真实数据接入或生产授权。

## [5.32.0] - 2026-09-06

### Added

- 确定性生成 schema.v1.15 / OpenAPI 1.12，完整聚合承接增量并内联闭合记录 schema；保留历史合同字节。
- 按来源 commit 的封闭 profile 导出合同，重建校验来源、生成物与前序/当前双哈希；历史 commit 仍可导出。
- 完整聚合 DDL 的隔离 PG15 验证入口；新旧导出、篡改与缺失来源负例进入回归。

### Scope

- 无新增 HTTP 路由；合同交接不代表产品接入、真实评测、部署或运行激活。

## [5.31.0] - 2026-09-06

### Added

- 发布/回滚传递承接版本、摘要与租户，实际 release 内容完整核对后才切换 current。
- current、搜索、分页与 ACK 通过既有读取门重验承接范围和生命周期；离线租约截止不超过承接到期时间。
- 合成集成覆盖 legacy 发布、撤销/到期、回滚、租约和不完整 release 拒绝，提供可选 5000 条真实存储门规模检查。

### Fixed

- 去除发布/回滚租户硬编码；归档限定本租户，过期承接内容允许仅归档而不修改其正文或身份。
- 读取承接完整性依赖不可变成员和数量证明，避免每次搜索重建整版正文摘要。

### Scope

- A2b-5 合成合同候选；正式合同导出、可信证据适配和产品消费仍待完成，不批准真实运行、T6 或部署。

## [5.30.0] - 2026-09-06

### Added

- 导入终结从已登记承接派生审核元数据，绑定实际条目后继版本及完整暂存范围，通过后才原子终结批次与租约任务。
- 合成 PG15 覆盖实际 worker 终结、质量/租约/权限拒绝、旧审核路径与批准范围漏项回滚。

### Fixed

- 移除质量证据函数对不可变计划的多余行锁，解决 definer 无 UPDATE 权限导致合法证据无法登记的问题；不新增权限。

### Scope

- A2b-4 导入终结候选；发布/回滚/current 与产品消费仍待后续接线，不开放真实运行能力。

## [5.29.0] - 2026-09-06

### Added

- 三处内容存储绑定负责人承接记录摘要，暂存行固定批准条目版本；实际行内容、条目成员、租户和最终摘要由数据库写入门核对。
- 承接有效期、来源就绪和撤销锁统一由内部生命周期函数持有，整批核对与存储写入复用同一判定。
- 合成 PG15 覆盖合法承接与历史 single/dual、重算摘要后的篡改、跨租户、撤销/暂停、撤回形状及应用角色直写拒绝。

### Scope

- 本次为 A2b-3 存储完整性候选。完整批次审核、导入 finalizer、发布/回滚/current 与版本化导出仍待后续接线；不开放运行能力。

## [5.28.0] - 2026-09-06

### Added

- A2b-2 内部候选核对从实际规范化内容和数据库来源事实重建承接范围，并绑定最终摘要、登记状态、负责人和批准证据。
- 合成 PG15 负例覆盖内容变化后重算摘要、条目增删、来源错配、撤销/暂停、跨租户和应用角色越权。

### Scope

- 只完成内部整批消费断言；调用方须在同一事务提供实际候选并完成业务验证。既有存储、发布和真实运行门尚未接线。

## [5.27.0] - 2026-09-06

### Added

- 承接内容摘要绑定规范化内容、条目版本、实际负责人、批准证据和承接记录摘要；任何一项变化均不能沿用原摘要。
- 隔离 PG15 与 Node 对照覆盖字段变化、非法元数据、权限拒绝及旧 single/dual 摘要语义。

### Scope

- 本次仅为 A2b-1 最终摘要候选；不证明批准真实性，不放宽内容存储、发布或真实运行门。后续消费者仍须验证有效承接及精确范围。

## [5.26.1] - 2026-09-06

### Changed

- 升级构建、无障碍检查与 OpenAPI 校验依赖：esbuild 0.28.2、axe-core 4.13.0、@redocly/cli 2.51.2。
- GitHub Actions 的质量与 Pages 工作流统一使用固定提交的 actions/setup-python v7.0.0。

### Scope

- DOMPurify 3.4.13 与 Mermaid 10.9.8 继续保持冻结；本次不迁移归档 vendor、SRI 或运行时内容，也不新增客服功能、权限或数据连接。

## [5.26.0] - 2026-09-05

### Added

- 可对单一负责人承接的四域版本与精确条目集合进行离线元数据校验，绑定独立批准摘要、负责人、风险和有效期。
- 新增隔离 PostgreSQL 15 合同基础：承接登记、追加撤销、同事务受控断言与审核前摘要，以及闭合 schema 和 OpenAPI 扩展候选。
- 新增纯合成验证，覆盖身份与租户隔离、来源与期限、权限、并发、撤销和规范摘要一致性。

### Fixed

- 合成数据库测试中断时共用停库与回收逻辑；SIGINT / SIGTERM 保留非零退出状态，停库失败时保留目录并报错。
- 未来时间负例改用执行时取时，避免前序测试耗时引发误报。

### Scope

- 仅交付 A1 / A2a 基础候选。冻结 SQL / OpenAPI、现有消费者和产品运行权限不变；A2b / B、真实评测、T6 与部署仍未完成或放行。
