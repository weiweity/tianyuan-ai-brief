# 客服 Agent · 费用与成本控制

> **项目：** 客服 Agent（首期：话术库 MVP-A）
> **配套：** [`00-项目章程.md`](00-项目章程.md) · [`02-G0责任与证据台账.md`](02-G0责任与证据台账.md)
> **用途：** 费用与采购沟通；工作方向已登记 2026-08-04 启动核验，执行路径、金额与 cap 由预算责任人回填
> **状态：** 启动前 / G0 · 公司正式凭证待补 · Ddev 未成立 · 软件未开发
> 规则：费用路径未正式留痕前，临时按 **B（钱后置）** 管理，不签约、不发生付费调用
> 口径边界：已收尾汇报 Web 中的组合预算数字只属于**历史会前提案**，不是客服已批预算，也不得自动继承为客服单项目 cap

---

## 1. 申请一句话

若 08-04 真实任务强制排序证明话术库 / 检索推荐为第一优先，再申请 AI 相关费用用于 **客服 Agent 首期话术库 MVP-A**（知识库、内容治理、问答与意图 / 检索推荐）；**不做自动代回**。排序前不提交付费承诺。

---

## 2. 为什么现在要钱（业务）

| 点 | 说明 |
|----|------|
| 已核实业务短期诉求 | 智能质检、评价分析报告、聊天分析 |
| 当前方案假设 | 话术库 / 检索推荐可能解决标准答法治理与查找问题；“外包、无内网、个人文档依赖”尚无本轮原始纪要证据 |
| 08-04 费用前置条件 | 一线运营角色候选（待核验）+ 话术 Owner 候选 + ≥3 坐席按统一评分卡复核四候选；话术库 / 检索推荐未第一、并列或缺证据时不按本表申请，先走 CR / DEC 或补证 |

---

## 3. 首期花在哪（科目）

| 序号 | 科目 | 必要性 | 预估金额（量级占位） | 备注 |
|------|------|--------|----------------------|------|
| 1 | 大模型 / API 调用费 | 问答与意图理解 | **数百～数千元/月**（精确数由预算责任人在 G0 回填） | 先跑关键词基线；智能方案有增益才申请；**必须**月度 cap |
| 2 | 应用托管 / 服务器 / 内网部署 | 内部可访问 | 并入首期小计量级 | 与 IT 口径对齐 |
| 3 | 检索/向量等相关组件（若需要） | 语义匹配 | 可与复用底座合并 | 优先复用 wording |
| 4 | 其他（域名、证书、存储等） | 视方案 | 视方案 | |
| | **MVP-A 首期小计** | | **待测算、待批准** | 不得用“万元内”替代具体单项目 cap；仅 MVP-A |

**库规模（控费与质量）：** 首期导入建议限试点场景精选（量级 ≤500 条，G0 与 Owner 共定），避免无主数据灌库拖垮检索与标注成本。

**按当前话术库假设明确后置：** 桌面端嵌入（L4）、外包全量环境、质检专项。若 08-04 强制排序由质检胜出，应先走 CR / DEC 重做首期范围和费用表，而不是把质检静默塞入本预算。

### 3.1 客服单项目成本模型（G0 必填）

| 成本项 | 单价 | 预计用量 | 月度小计 | 全期小计 | 计量来源 / Owner |
|--------|------|----------|----------|----------|------------------|
| 模型输入 / 输出 | | | | | |
| 向量 / 检索 | | | | | |
| 托管 / 算力 | | | | | |
| 存储 / 日志 / 备份 | | | | | |
| 第三方账号 / 证书 | | | | | |
| **合计** | | | | | |

| 预算层级 | 状态 | 金额 / cap | 规则 |
|----------|------|-------------|------|
| 客服 Agent MVP-A 月 cap | 待 G0 批准 | ____ | 只能计客服 `project_id` |
| 客服 Agent MVP-A 全期 cap | 待 G0 批准 | ____ | 不得高于正式批准金额 |

旧客服 + 供应链组合预算仅作历史来源追溯，不在本表展示或授权；供应链预算必须在未来独立项目中另行申请。

---

## 4. 费用与交付挂钩（给审批人吃定心丸）

| 里程碑 | 费用使用 | 可停损 |
|--------|----------|--------|
| D0 / G0 | 只做费用测算、现状盘点与无付费评测设计 | 正式路径与 cap 未批则 0 新增工具支出 |
| MVP-A 可演示 | 开发期调用 + 托管 | 演示不过可停扩面 |
| 内部小范围试用 | 试用期调用 | 无采用则不进 L3/L4 追加预算 |

---

## 5. 不批 / 少批时的降级方案

| 情景 | 降级 |
|------|------|
| 不批模型费 | 仅做结构化话术库 + 关键词检索（无智能问答） |
| 不批托管 | 仅方案与本地 Demo，不承诺业务试用 |
| 只批极少额度 | 缩到单场景话术包 + 人工维护，验证流程后再扩 |

---

## 6. 执行路径（G0 补正式留痕）

> **公开仓规则：** 本文件在公开仓只保留空模板、路径状态和 `EVD-*` ID，不填写真实 cap、批准原文或原始链接。若选择 A，必须先把 00–06 迁到私有仓 / 受控副本，再在私有副本填写金额并运行校验；公开副本继续保持未授权模板。

> 客服 Agent 的 2026-08-04 启动安排已登记；先按统一评分卡核验四候选，话术库 / 检索推荐证据充分且明确第一时本节才确认谁付、从哪付、cap 是多少。本节不能替代公司正式批准凭证。

- [ ] **A 费用可用：** 预算责任人（待具名）按客服单项目 cap 批准，并与公司正式批准凭证分别留痕
- [ ] **B 费用后置：** 转 ________ 部门 / 预算科目，金额未批前保持 0 支出
- [ ] **B 费用后置：** 走公司 IT / 数字化统一采购，金额未批前保持 0 支出
- [ ] **C 暂停执行：** 原因 ________；下次复核日期 ________

| 必填批准项 | 结果 |
|------------|------|
| 预算 / 费用责任人（仅 1 人） | |
| 客服项目月 cap | |
| 客服项目全期 cap | |
| 费用科目 / 采购路径 | |
| 预警阈值与通知人 | |
| 超线停扩授权 | |
| 批准人代号 / 日期 / 决定摘要 / 证据 ID | |
| B 下次费用决策日 | |

---

## 7. 联系人

| 角色 | 人员 / 角色代号 |
|------|-----|
| 项目主 R | ROLE-PROJECT-FDE（待分配具体人员） |
| 公司批准人候选 | 待核验角色权限、待分配人员代号（目标 2026-08-05） |
| 业务接口 | 客服业务 Owner 候选（待核验、待具名） |
| 财务 / 预算责任人 | 待具名（目标 2026-08-05） |

*费用与成本控制 v3.0 · 2026-07-31 · 08-04 启动基线*
