# A3 · 负责人承接完整合同交接

> **状态：** `VALIDATED · SOURCE_COMMIT_HANDOFF_PENDING · NOT_ACTIVATED`
> **范围：** A1 / A2a / A2b-1～5 的完整版本化合同；产品消费、真实装包、评测与部署分别留证。

- **直接前序机器合同：** schema.v1.14 `edf909bf9450b5745a85ced4a75a2e2de3e5b061847562cd3a68c9c7c226da99`；OpenAPI 1.11.0 `06698f233702591c8f981c7b08ebac4b7d5bc5cc2d69d36014ef2a9f5a6802e4`。
- **A3 机器合同增量：** schema.v1.15 `859c4a4757d87e642e797ad8a26cfb334c49ae7f8f263966099eb89e6750b38b`；OpenAPI 1.12.0 `361f20128c88143eb87370f136b67c4de8c1ed5fc02c7072f62c16545951315c`。
- **实际产物必须精确匹配：** DDL `859c4a4757d87e642e797ad8a26cfb334c49ae7f8f263966099eb89e6750b38b`；OpenAPI `361f20128c88143eb87370f136b67c4de8c1ed5fc02c7072f62c16545951315c`。

## 来源与生成

[DEV-M1 增量](01-DEV-M1搜索合同增量.md) 与 37 / 39 / 46 的冻结声明保持原义和字节。新增 `contract-profile.v1.json` 在明确来源 commit 内选择承接合同；无 profile 的历史 commit 继续使用旧合同，未知 profile 拒绝导出。

[生成器](../../08-工具/build_customer_agent_owner_contract.mjs) 唯一拥有聚合顺序：旧 reference DDL → registry → content-hash → content-scope → storage → import → publish。每段保留原始 SQL 与来源 SHA-256；末尾独立更新数据库 schema 标识至 v1.15，生成 [schema.v1.15.sql](schema.v1.15.sql)。这是全新数据库参考 DDL；产品必须保留旧 migration，追加后缀迁移，禁止当作现有数据库原地升级脚本。

[OpenAPI 1.12](openapi.v1.12.yaml) 从旧版本生成，只增加 ReviewMode 的 owner_acceptance、闭合 OwnerAcceptanceRecord schema 与登记 vendor extension。schema 内联 canonical JSON schema 的本地引用，导出无需第三个依赖文件；不增加 HTTP 路由，不暴露批准记录到普通响应。`runtimeActivated=false`、`g1a_offline_only` 保持。

## 可复核交接

运行 `node business-docs/08-工具/build_customer_agent_owner_contract.mjs --check` 检查派生字节。导出器先完整核对旧规范链，再核对本页前序及当前双哈希，并从同一 commit 的 canonical 输入重建比较；工作树内容不能代替 Git blob。

Git 留存后，按完整 40 位 `source_git_sha` 和本页双哈希调用既有 export 命令，实际生成只读三文件合同集。`contract_set_id` 由 OpenAPI / DDL 版本与真实来源 commit 确定，不能预填未存在的 SHA。产物放在 ignored `output/customer-agent-contract-sets/`；产品显式接收并锁定该来源后才能开始消费验证。堆叠 PR 的精确 commit 可追溯，但不得称作 main 已合并。

## 验证与限制

2026-09-06：确定性生成检查 PASS；合同导出 14/14 与聚合/schema 测试 2/2 PASS；完整聚合 DDL 的隔离 PG15 承接集 53/53 PASS（含实际 schema 标识及中断清理）；完整 release PASS，Node 213/213、业务视图 49/57/30、架构 37/37、PlantUML 8/8、依赖 audit 0。新版 OpenAPI lint 通过，2 个未引用组件提示分别为离线记录和既有 worker 行合同，无新增 HTTP 路由。独立审查的合并历史降级问题已通过失败复现、`--full-history` 修复及合并拓扑负例闭合。Git 留存及真实来源导出在最终提交后执行，以实际产物为准。登记信任锚点必须来自单独核验的外部批准记录；合成验证不签发真实批准，也不代表 T5 / T6 或上线通过。
