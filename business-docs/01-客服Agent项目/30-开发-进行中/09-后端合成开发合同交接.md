# 后端合成开发合同交接

> 状态：FROZEN · SYNTHETIC DEVELOPMENT ONLY；批准 DEC-BACKEND-SYNTHETIC-20260909，等待固定提交导出及产品 intake。

**直接前序机器合同：** DDL `859c4a4757d87e642e797ad8a26cfb334c49ae7f8f263966099eb89e6750b38b` / OpenAPI `361f20128c88143eb87370f136b67c4de8c1ed5fc02c7072f62c16545951315c`。

**后端机器合同增量：** DDL `0db44d4d44e968b24e90dda8bcd26a077dd33395ff5a31efb38d085254d4c44f` / OpenAPI `c3c14659261ed01ff4f0c187026601844f59d3cd26be605a34f647bc130cc94c`。

**实际产物必须精确匹配：** DDL `0db44d4d44e968b24e90dda8bcd26a077dd33395ff5a31efb38d085254d4c44f` / OpenAPI `c3c14659261ed01ff4f0c187026601844f59d3cd26be605a34f647bc130cc94c`。

固定生成器组合旧基线与 backend-runtime-candidate 的身份、审核和事务增量，产出 OpenAPI 1.13.0 / schema.v1.16。旧文件保持不变。DDL 是 clean-install reference，产品以不可变新增 migration 接收，不得对已有数据库重放完整 reference。

新 bearer 为产品自有不透明会话；旧 mock 只用于显式合成配置。当前只批准单主机合成开发测试，runtime_activated=false；真实提供方、数据、Windows、部署另行批准。原 candidate 文件保留来源身份，不以标签替代正式组合验证。

导出只从通过审查、验证并提交的固定来源生成，绑定双哈希与 contract_set_id。产物可消费验证完成后更新本页交付状态，不能凭本页声明推定已冻结。

验证：确定性生成、固定提交导出及防降级回归、完整发布检查通过；组合 SQL 已通过隔离 PG15 安装、角色行为与双连接事务锁检查。OCR delegate 对 9 个文件完成宿主审查，批准状态子串误判已修复并复核；不冒充外部 Grok 审查成功。规模、真实身份、产品运行整链仍由后续验收负责。
