# 客服 Agent · Grok `/autoplan` 双模型对抗终审

> **历史快照 / 已被替代：** 本文件记录 120 分钟需求会约束下的上一轮静态验收。2026-08-01 用户将 08-04 需求会硬限制为 60 分钟，因此本文的议程结论、10.0 分与 GO 不得继续作为会议主持依据。现行结论见同目录 `2026-08-01_60分钟需求会双CLI对抗复审.md`；当前业务状态仍以 00–06 真源为准。

> **日期：** 2026-08-01
> **会议：** 2026-08-04 客服一期需求会
> **当前结论：** 项目已批准进入需求与方案阶段；可以开需求会，**不能宣布开发开工**。
> **评分边界：** 10.0 只评价本仓当前文档、代码契约与自动化浏览器证据；不替代会前业务资料、现场真机、G0、Ddev、灰度或经营结果验收。

## 1. 当前唯一口径

| 事项 | 现状 | 不得误读为 |
|---|---|---|
| 公司批准 | 已归档受控证据 `EVD-CUSTOMER-APPROVAL-20260801` | 一期功能、费用或技术栈已批准 |
| 一期方向 | 08-04 用真实客服任务确认 | 已预选知识库、话术库或任一功能 |
| 外部责任包 | **1 / 14 Pass** | G0 已通过 |
| Scope | **1 / 15 Pass** | 范围与验收已冻结 |
| 开发状态 | G0 未签发；Ddev 未成立；产品代码未创建 | 08-04 是编码第一天 |
| 费用 | 临时 B；新增付费 0；正式路径未签 | 预算已经批准 |
| 旧学习项目 | `dafuyan-wording` 永久不复用、不审计、不迁移 | 可作为代码、数据、架构或工期依据 |
| 已收尾 Web | 只参考配色与表达方式 | 客服产品代码底座 |

## 2. 第一轮 Grok `/autoplan` 对抗结果

Grok CLI 以只读、禁用联网和高推理模式审阅产品、客服、BP、工程、UI、UX 与内容一致性。第一轮结论为 **6.8 / 10，CONDITIONAL GO**。

| 视角 | 初始分 |
|---|---:|
| CEO / 经营 | 7.4 |
| 客服总监 | 7.1 |
| 客服经理 / 坐席 | 6.6 |
| 客服 BP | 6.9 |
| 产品经理 | 7.3 |
| 全栈工程 | 7.6 |
| UI | 6.4 |
| UX | 6.8 |
| 内容一致性 | 5.2 |
| **算术均分** | **6.8** |

### 第一轮发现与处理

| 级别 | 对抗发现 | 处理结果 | 防回归证据 |
|---|---|---|---|
| P0 | `02`、`06` 与执行中心存在两套 120 分钟议程 | `02` 成为唯一议程真源；`06` 必须逐行同构 | 新增 `customer_project_meeting.mjs`，8 行或起止时间漂移即失败 |
| P0 | 旧 DEC-003 预选“话术库 MVP-A”并要求排序 | DEC-003 明确废止；DEC-009 禁止预选、功能投票和现场选技术栈 | 当前契约主动拒绝旧产品名、独立预评分和强制排序 |
| P0 | 测试仍把旧 MVP-A 与旧无障碍名称当正确答案 | PRD、Hub、状态与历史导航断言全部改为现行需求会语义 | Node、Chrome、HTTP、file、Back / Forward 全链路通过 |
| P0 | 旧 10.0 报告仍写“未批准、0/14、0/15” | 本文件已原位替换旧快照 | 项目 README 与评审索引只指向本现行报告 |
| P0 | 会前真实样本、指标、来源、Owner、试点尚未填实 | 保持 `OPEN`，明确 08-03 18:00 截止；缺项时会议只能做事实发现 | 不把外部输入缺失伪装成仓内完成或 D1 冻结 |
| P1 | 一页卡、章程与知识指标暗含预设方案 | 一页卡改成非技术会议卡；知识指标全部改为“仅当该方向胜出” | PRD / Hub 双页与 7 份真源同步检查 |
| P1 | 页面标题更新后，历史 UI 回归仍等待旧标题 | 统一为“需求会项目说明 / 需求会执行中心” | 四视口及往返、刷新、前进后退、离线链路已实测 |

### 第二轮复核：9.4，不接受自报满分

Grok 第二轮确认主链修复成立，但发现同名 `evidence.json` 仍写旧的 **0 / 14、0 / 15、0 / 29**，同时自报 P0 / P1 为 0，因此给出 **A = 9.4、B = 9.1、C = 不允许开工**。该反对意见成立，本轮继续完成了三项收口：

1. 机器证据升级到 v2.0，并改为现行 **1 / 14、1 / 15、2 / 29**；
2. 评分维度由旧七维统一为九视角；
3. 新增真源对终审 JSON 的失败关闭测试，模拟旧 0 / 29 时必须报错，不能再次冒充 10.0。

### 第三轮封口：静态 10.0

Grok 第三轮只读复核了终审 Markdown、机器证据、状态推导与负例测试，结论为：**P0 = 0、P1 = 0；九视角均为 10.0；A 静态分 = 10.0；B 开会准备度 = 9.3；C = 不允许开工**。它列出的两个不扣分发现也已收口：评审索引增加机器证据入口，外部责任包、Scope、总计三个字段分别做单字段漂移负例。

Grok 所在运行环境没有可用的 Playwright Chromium，因此第三轮没有自行重跑浏览器链；这不冒充它已实测。本地主审随后完整执行 `npm run test:all`，当前证据为 91 / 91、UI 全链路通过、PRD 47 / 47、Hub 53 / 53。

## 3. 修复后静态复验

以下分数只覆盖可在本仓复现的证据，不把外部事实混进平均分。

| 视角 | 分数 | 达到 10.0 的依据 |
|---|---:|---|
| CEO / 经营 | **10.0** | “已批准 → 选主问题 → 冻结成败 → 决定是否开工”故事线单一，费用与供应链不混项 |
| 客服总监 | **10.0** | 只要求决定业务问题、成功 / 停止、负责人和一期边界，不要求选择框架 |
| 客服经理 / 坐席 | **10.0** | 用真实工作与通俗问题主持；无功能投票、无工程术语决策 |
| 客服 BP | **10.0** | 会前输入、会议记录、Owner、证据位置和下一决策时间均有落点 |
| 产品经理 | **10.0** | D0、D1、G0、Ddev、Pilot 各阶段不混读，In / Out 与变更规则完整 |
| 全栈工程 | **10.0** | 绿地隔离、ADR 输入、身份 / 数据 / 安全 / 容量 / 回退门禁齐全，未提前建产品代码 |
| UI | **10.0** | 克制配色、桌面与 320–1440 宽度回流、dark / reduced-motion、打印与无横向溢出通过 |
| UX / 无障碍 | **10.0** | 非技术首屏、键盘、焦点、触控目标、对话框、复制反馈、导航历史与 Axe 门禁通过 |
| 内容一致性 | **10.0** | 7 / 7 真源、双页稳定点、议程逐行同构、状态与术语失败关闭 |
| **算术均分** | **10.0** | **最低分 10.0；静态 P0 = 0，P1 = 0** |

## 4. 可复现证据

| 检查 | 结果 |
|---|---|
| `npm run test:all` | **exit 0** |
| Node 全量契约 | **91 / 91**；含终审 JSON 对真源的失败关闭负例 |
| PRD 浏览器 QA | **47 / 47**；证据目录 `output/customer-agent-prd-qa/round-ci-6Mnbg3` |
| 执行中心浏览器 QA | **53 / 53**；证据目录 `output/customer-agent-hub-qa/round-ci-knhGP4` |
| 历史 Web UI | 375、390、1366、1440 四视口；Axe A / AA 违规 0 |
| 布局稳定 | 移动端 CLS 0；桌面端 0.008985 / 0.007308 |
| 议程真源 | `02` 与 `06` 8 / 8 行逐项一致；负例漂移会失败 |
| PRD / Hub 真源 | **7 / 7 + 7 / 7**；稳定点 `hub-v1-c9a297d5ff57` |
| 公开 / 私有边界 | **7 / 7**；真实私有迁移、路径转义与拒绝覆盖通过 |
| 依赖审计 | **0 vulnerabilities** |
| 差异格式 | `git diff --check` 通过 |

复现入口：

```bash
node business-docs/08-工具/sync_customer_agent_surfaces.mjs --check
cd web-decision-brief
npm run test:all
```

相关实现：[双页同步器](../../08-工具/sync_customer_agent_surfaces.mjs)、[会议议程契约](../../08-工具/customer_project_meeting.mjs)、[PRD QA](../../08-工具/test_customer_agent_prd.mjs)、[Hub QA](../../08-工具/test_customer_agent_hub.mjs)、[历史 Web 回归](../../../web-decision-brief/tests/ui-audit.mjs)。

## 5. 仍为 OPEN 的真实业务与现场事项

这些不是静态文档缺陷，也没有被 10.0 冒充为已完成：

- 08-03 18:00 前收齐脱敏真实任务、现状指标与分母、权威来源及权限、3–5 名试点、预计人数与峰值；
- 08-04 确认一期唯一主问题、业务 Owner、内容 / 话术 Owner、成功线、停止线与反对意见；
- 会后冻结技术 ADR、费用路径、安全批准、WBS、评测协议和 Ddev；
- 实体投影、真实会议网络、iOS / Android 真机、VoiceOver / TalkBack、打印机仍需现场验收；
- 软件上线、两周灰度与经营提效必须用真实数据另行验收。

若会前资料缺失，08-04 仍可开事实发现会，但必须把对应结论记为 `OPEN-*`，**不得宣称 D1 冻结，更不得宣布开发开工**。

## 6. 决策

**GO：** 允许按本材料召开 2026-08-04 客服一期需求会。
**NO-GO：** 不允许把本次审计解释为技术栈已选、费用已批、G0 已过、Ddev 已成立、软件已上线或灰度已通过。

---

*终审记录 v2.0 · Codex 主审 + Grok CLI `/autoplan` 对抗审核 · 分数上限 10.0，不用“10 分以上”制造伪精度*
