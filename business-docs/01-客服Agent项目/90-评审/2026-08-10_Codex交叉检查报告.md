# 2026-08-10 · Codex 交叉检查终版报告

> **生命周期：** FINAL DESIGN-CLOSURE REVIEW / NON-NORMATIVE EVIDENCE；本报告冻结到 `90-评审/`，只证明 2026-08-10 的设计收口与当次验证结果，不承担当前架构或实现真源。现行合同以 `20-设计-进行中/37`、`39`、`40`、`46` 为准。

> **项目：** CS-AI-C11 客服 Agent 一期\
> **日期：** 2026-08-13 · v1.41\
> **Phase A 结论：** **PASS-WITH-CONDITIONS（静态设计）**；D1 已改为自动事实 + Pilot 离线修改/发送/正确性三维抽样；扩展治理已补齐签名客户端 N/N-1 兼容周期、只读 PlatformAdapter 与 DEV-M0 迁移兼容矩阵；CR-003 仍只预埋 49/50 离线训练边界；CR-004 冻结不可变来源版本、每 release 四域 binding 与导入/发布/回滚/检索 fail-closed；DEC-042 人读与静态机器合同已锁，迁移/runtime/动态证据未实现\
> **Phase B 结论：** **实现设计已 Pass · Document Package Ready**，见 [`46-实现设计-开工包.md`](../20-设计-进行中/46-实现设计-开工包.md)\
> **PG15 设计前置验证：** 当前 schema v1.12 clean-install reference DDL（SHA-256 `ed5f982248e38aead3b23a7ce235cedf7e7e15266df9fade146e932de648fd7f`）已在本机隔离 PostgreSQL 15.18 通过 40 tables / 2 views / 143 functions、ACL 8/8、约束 3/3、完整幂等重跑与 COMMIT 前故障原子回滚，结果 **PASS-WITH-LIMITATION**，证据 `EVD-PG15-LOCAL-PREFLIGHT-20260810T055834+0800-ED5F9822`；immutable migration / N/N-1 / application runtime / managed PG / backup-restore / concurrency-deadlock / production 仍 **NOT_CERTIFIED / NOT_IMPLEMENTED**\
> **开发结论：** **BLOCKED BY Ddev**；本报告不是 G0/Ddev 签发，未授权 Phase C 业务代码、部署、试点或付费调用\
> **责任入口复核：** 13 个角色已在 `02` §5 的唯一 RACI 表完成主责 / 代理代号、职责接受、受控 EVD 与生效日期登记，统一证据为 `EVD-RACI-ACCEPTANCE-PACK-20260810`（内容 Owner 保留原接受证据并追加本包）；七类 Owner 只从该表固定投影，不另建第二套名单。该接受包只证明职责接受，不替代业务、费用、安全、运维等责任包的事项签发。\
> **Ddev 授权复核：** `DEC-DDEV-01` 已设为独立机器硬门；仅当状态与结论均为 `PASS`、G0 `14/14`、Scope `15/15`且完整签发包一致时，Ddev 才能成立。当前仍 `PREPARED · NOT ELIGIBLE / NOT AUTHORIZED`，G0 `13/14`、Scope `14/15`、Ddev 为空、代码未开始。
> **G0 可签包复核：** `EVD-G0-07-FEE-PATH-20260813` 与 `EVD-G0-14-WBS-CAPACITY-20260813` 已原子关闭 G0-07 / G0-14 及 Scope #11 / #15；当前合计 `27/29`，仅 G0-09 / Scope #9 待关闭。G0 仍未签发且 Ddev 为空。

---

## 0. 一页结论

| 问题 | 结论 |
|------|------|
| 路线是否重开 | **否**。Route A、Windows Electron + Node/TS API + PostgreSQL 继续冻结 |
| 产品红线是否被弱化 | **否**。剪贴板主 CTA、禁代发、一期禁改写、有效期强制、只有 push 成功计 adopted 均保留 |
| 一期工单是否入 Dashboard | **是，按 CR-001 的只读导出分析方向。** 业务“工单分析”=`work_order_*`；内部“话术优化待办”=`iteration_task*`，分名、分表、分 API、分权限；书面 EVD / G0 / Ddev 仍待补 |
| 初始规格是否无洞 | **否**。发现过权限、回滚、DEFINER ACL、幂等/失租、withdraw、中文检索落点与现行入口旧 Demo 口径等 P0 |
| 修补后规格是否可交给实现者 | **是，限设计/静态口径**。上述 P0 已回写 33/37/39/41/43/46 并加护栏；DEC-042 已完成 schema/OpenAPI 静态机器闭环；44 只保留为历史评审快照 |
| 是否现在可写业务代码 | **否**。费用、WBS / 容量 / 成本与运行交接方案已签发，但 G0-09 / Scope #9、正式 G0 与 DEC-DDEV-01 仍未关闭 |
| G0-14 / G0-15 是否已有可签草案 | **均已 Pass，Scope #15 已勾选。** `EVD-G0-14-WBS-CAPACITY-20260813`、`EVD-G0-07-FEE-PATH-20260813` 与 `EVD-G0-15-RUN-HANDOVER-20260812` 已归档；不冒充真实运行或 Ddev 授权 |
| 是否已证明 PostgreSQL/HTML 真机可用 | **分账判断。** v1.27 已完成 PlantUML/SVG/HTML 7/7 确定性重建与 Chromium 四视口自动检查，移动端瀑布图首屏、键盘、缩放与 Axe 均通过；这仍不等于真实 Windows 安装包、投影、触控或真读屏验证。当前 schema v1.12 reference DDL 本机隔离 PG15 预检已 `PASS-WITH-LIMITATION`，但迁移、应用运行态、托管库、并发/死锁、备份恢复与生产仍未认证 |
| 权威来源是否已能由系统硬拦截 | **否，只有静态合同。** CR-004 已把来源版本、四域 binding、暂停、原子发布/回滚、检索与离线 lease 拒绝路径写入设计与静态机器合同；四个真实来源与 EVD 未齐，迁移、API/PG/客户端 runtime 也未实现或实跑，G0-09 仍进行中 |
| 是否可提交到当前 origin | **否，P0 发布门待 DEC-PUBLISH-01。** 当前 GitHub origin 为 Public；须对**整个 changeset**给出 Product/Security/Tech exact-file manifest、脱敏与 Proprietary/license 批准。仓内外仍有未核准受控资料（本轮未读取、不声明精确数量），禁止 `git add .` |

## 1. 已读材料与证据等级

### 1.1 全文读取

1. 外部交接附件 `pasted-text.txt`（只作输入，不是仓库现行 SSOT）
2. `37-架构SSOT-v1.md`
3. `39-API合同与发布状态机-v1.md`
4. `33-schema-v1-草案.sql`
5. `41-NFR扩展并发与防改崩.md`
6. [`2026-08-06_架构交叉验证终裁快照.md`](2026-08-06_架构交叉验证终裁快照.md)（历史评审证据，非现行 SSOT）
7. `43-技术栈全景清单-部署向.md`
8. `32-ADR-一期技术栈.md`
9. `31-产品契约-v1.md`
10. `29-Dashboard产品说明.md`
11. `25-PRD草案-客服Agent一期.md`
12. `40-架构图与关卡状态.md`
13. `README.md`
14. `架构图-PlantUML浏览器.html`、`diagrams/*.puml`、`tests/test_arch_ssot_invariants.py`
15. 组合现行入口 `business-docs/00-项目驾驶舱.md`
16. 项目真源 `02-G0责任与证据台账.md`、`03-Scope与验收.md`、`05-全栈交付计划.md`
17. D0 证据、需求完成目录、参考目录、评审索引与历史设计包清单
18. 路线拍板历史 [`28-自研vs中台WBS对照.md`](../99-历史/2026-08-06-架构设计收口/28-自研vs中台WBS对照.md)

### 1.2 证据等级

| 等级 | 本轮完成 | 能证明 | 不能证明 |
|------|------------|--------|------------|
| 文档语义 | 全文对照 + 冲突修补 | 合同是否自洽、实现者是否少发明 | 运行时真实行为 |
| 结构护栏 | v1.23 / v1.26～v1.33 仅作历史快照；v1.34 是最近一次全量回归；v1.37 针对 G0-12 / Scope #13 状态增量实跑 **37/37 PASS** | 当前高价值文本/结构不变量、受控 search、Question key version/不可变版本、异人双审、质量证据信任边界、denial wrapper ACL、13 角色唯一 RACI、七类 Owner 投影、G0-08 / G0-10 / G0-11 / G0-12 证据、DEC-DDEV 独立授权门、Gate/Scope 成对推进与 37/46 精确版本锁均由当前静态护栏证明 | SQL 在 PG15 一定可执行，或 runtime 已实现 |
| 机器 API 合同 | **Redocly PASS**；OpenAPI 3.1.1 / API 1.11.0，29 paths / 29 operations（27 条 `/v1` + 2 探针），SHA-256=`0dba20e22a7bba5a5bf93ad7383e42be97b0da3f7b5953860ae226823af016be` | 静态 wire 已与 39 对齐；Phase 1 伪造 native_integration 必须 403+零写入；CR-004 来源 binding/短 lease 与 DEC-042 public wire 白名单已锁；无新增审核主体 ID、在线复核或训练 API | API runtime 已实现或响应符合合同 |
| HTML / UI 自动化 | **PASS**；7 Tab、ARIA、键盘、可读/总览模式、离线资源、Chromium 4 视口 × 7 Tab、Axe 0 违规；默认首屏出现非标题图内容；PlantUML 7/7 源/产物/HTML 同步 | 浏览器自动化下的可达性、响应式与生成一致性 | iOS/投影/触控/真读屏效果 |
| DDL 静态 | **SQL grammar PASS**；当前本地 workspace 已安装并锁定 `@libpg-query/parser@17.6.10`；schema v1.12 的冻结 grammar 形状为 513 条 SQL 语句 + 89 个函数体 + 20 个 DEC-042 guard，SHA-256=`ed5f982248e38aead3b23a7ce235cedf7e7e15266df9fade146e932de648fd7f`；grammar SHA-256=`30d4aff357f567bced786d89c684c13b990ba878cc79aebb328a2c3d46b695a1` | 当前本地 workspace 可复跑 PG 方言 grammar gate；CR-004 来源围栏与 DEC-042 受控 search、Question 版本、审核决定及质量 plan/evidence 已进入静态机器合同 | parser 单独不能证明真 PG15 行为 |
| DDL · 本机 PG15 运行 | **PASS-WITH-LIMITATION（current reference DDL preflight only）**；PostgreSQL 15.18，40 tables / 2 views / 143 functions，ACL 8/8，约束 3/3，完整幂等重跑与 COMMIT 前故障原子回滚通过；`EVD-PG15-LOCAL-PREFLIGHT-20260810T055834+0800-ED5F9822` | 当前 schema v1.12 clean-install reference DDL 的本机隔离前置形状 | immutable migration / N/N-1 / application runtime / managed PG / backup-restore / concurrency-deadlock / production 仍 `NOT_CERTIFIED / NOT_IMPLEMENTED`；证据是本地 Git ignored 产物，fresh clone 须按 [08 工具入口](../../08-工具/README.md) 重跑 `npm --prefix sites run preflight:customer-agent-pg15` |

## 2. 交叉检查表

- [x] 37：Demo ≠ 架构、禁代发、浮窗 + Dashboard、PostgreSQL SoR
- [x] 39：九端口、错误码、幂等、owner-only publish/rollback、`work-orders` / `iteration-tasks` 分域、INV-∗
- [x] 33：PostgreSQL 方言、MERGE/withdraw、rollback、幂等、rate limit、内容/工单 outbox fencing、两类工单独立表与 ACL
- [x] 41：容量/池/限流/SPI、只读 PlatformAdapter、fail-closed、同事务/fencing
- [x] 扩展治理：39 的 N/N-1 与 /v2 边界、41 的平台候选隔离、46 的迁移兼容矩阵已进入跨文档机器护栏
- [x] CR-004：不可变来源版本、四域 release binding、永久暂停、全快照发布/回滚校验、检索/离线 lease fail-closed 已进入跨文档与机器草案；不冒充运行实现
- [x] DEC-042：`search_recommendable_scripts` 唯一运行检索、Question origin key version/不可变版本、固定角色异人双审、`content_review_decisions` 与质量 plan/evidence capability 分账已进入人读与静态机器合同；public wire 不暴露审核主体 ID，不冒充 runtime
- [x] 44 历史补洞输入中的 bigram、STRIDE、SLI、故障表、RTO/RPO 已吸收到 37/39/41/47/49；44 不再承担规范效力
- [x] 43 与 32：路线/部署/认证真源已对齐
- [x] HTML：7 Tab 顺序、Tab5 竖排、Tab7 映射、键盘/ARIA、切 Tab 后 contain fit
- [x] 隐藏 panel：不在 0 宽高上计算 fit
- [x] 护栏：v1.23 / v1.26～v1.33 仅作历史快照；v1.34 保留最近一次全量回归；v1.37 针对 G0-12 / Scope #13 增量实跑 **37/37 PASS**，继续覆盖 CR-002、CR-003、扩展治理、CR-004、DEC-042、workload-specific denial wrapper ACL、13 角色职责接受、G0-08 / G0-10 / G0-11 / G0-12 证据、四类核心主责 / 代理八代号分离、七类 Owner 投影、DEC-DDEV 独立授权门与 Gate/Scope 成对门
- [x] 无“设计 10 分 = 已上线”污染
- [x] Chromium 桌面/移动视口、Axe、键盘/hash/fullscreen/reduced-motion 自动化
- [x] Tab6 默认首屏空白回归已改为桌面可见 L0–L3、移动可见 L0–L2；移动 Tab 单行横滑；持久 UI 门禁检查非标题内容可见
- [x] 当前 schema v1.12 reference DDL 本机隔离 PG15 预检：**PASS-WITH-LIMITATION**；SHA / EVD / 对象数 / ACL / 约束 / 幂等 / 原子回滚已登记，不冒充 migration/runtime/production 证据
- [ ] 上一发布版本升级、托管 PG、并发/死锁、真设备/读屏/投影、真 OAuth、产品 E2E、压测、备份恢复与生产演练：**尚未认证**

## 3. P0 清单（初始发现，本轮已修）

| ID | 现象与初始风险 | 证据 | 修补 | 最终状态 |
|----|------------------|------|------|----------|
| P0-01 | 一期 `rewrite/auto_send=true` 曾可由 Owner + ADR 越过，直接违反一期红线 | 37 §3/5、39 §7、41 §4.3 | `/v1` 无条件 `POLICY_DENIED`；33 `set_policy_flag` 同步硬拒；未知/部署态 key 拒写 | **CLOSED · 规格/静态** |
| P0-02 | publish 权限在 coach/owner 之间漂移；rollback 可直指回旧 release，缺完整函数 | 31 角色、37 §4、39 §5、33 | 一期仅 Owner；rollback 复制目标 snapshot 为**新 release_seq**，current + announcement + audit 同事务 | **CLOSED · 规格/静态** |
| P0-03 | SECURITY DEFINER 默认 PUBLIC EXECUTE、权限仅注释、actor 参数易被误称为 DB 真实身份 | 33 ACL 尾部、41 §4.2 | 固定 `search_path`；显式 revoke PUBLIC；分离 definer/runtime role；明确 actor 仅服务端已验 claims 的审计值 | **CLOSED · 规格/静态** |
| P0-04 | DDL 曾 `DROP TABLE IF EXISTS idempotency_keys/outbox_jobs`，重跑会丢状态 | 33 旧版 NFR 表段 | 改为 `CREATE TABLE IF NOT EXISTS` + 向前 `ALTER`；加负断言禁止回归 | **CLOSED · 静态** |
| P0-05 | 幂等 claim/业务/complete 崩溃窗口；旧 worker 可覆盖新 worker | 39 §0.2、41 §2.5.1、33 idempotency/outbox | 同步写同事务；跨事务加 `lease_owner + lease_version`；outbox claim/heartbeat/complete 验证失租并剥夺 runtime 直写权 | **CLOSED · 规格/静态** |
| P0-06 | import 返回 202 时 outbox 非必须；MERGE 无显式下架语义 | 39 §5、33 import/publish | batch + `import_validate` outbox 同事务持久化才返回 202；增 `upsert|withdraw`、cancel、完整 MERGE | **CLOSED · 规格/静态** |
| P0-07 | 中文 2-gram 曾与 `pg_trgm` 混为一谈，且 DDL 无主路落点 | 44 §3.1、39 §2.1、33 release_items | 冻结 Unicode code point bigram + NFKC + A/B/C 权重 + GIN `tsvector`；`pg_trgm` 仅 fallback 加速 | **CLOSED · 规格/静态** |
| P0-08 | adopted 只校验 push_method，不足以防他人 query/伪造候选 | 39 §3、33 adoption FK | 绑定 query owner；adopted 必须精确匹配 `candidate_impressions(query_id,rank,script_id)` | **CLOSED · 规格/静态** |
| P0-09 | 组合驾驶舱仍写“自动填优先/演示可不做过期”，G0 台账写“架构未冻”，28 仍像现行执行清单 | `business-docs/00-项目驾驶舱.md`、`02`、`28` | 驾驶舱改回剪贴板主 CTA + 有效期强制；DEC-025 标历史并指向 31/37；28 标 `SUPERSEDED`、当前执行改 46；加入入口护栏 | **CLOSED · 文档/静态** |
| P0-10 | import worker 重试耗尽时只把 job 收口为 dead，batch 可永久卡 validating | 33 v1.3 outbox claim/retry | 33 v1.4 专用 retry + claim 前 reconciler，batch→outbox 锁顺序同事务写 `job=dead + batch=failed + error_report`，递增 fencing；结构护栏锁死 | **CLOSED · 规格/静态** |
| P0-11 | 新增业务工单导入最初只有 claim/heartbeat/finalize；通用 outbox 会把耗尽 job 标 dead，却可能让 `work_order_import_batches` 永久停在 validating | 33 v1.5 `work_order_import_validate` 路径 | 33 v1.6 增工单专用 retry + claim 前 reconciler；通用 claim/retry/complete 显式拒绝两个 import 域；按 batch→outbox 锁序原子写 `job=dead + batch=failed + diagnostic_id`，并以 capability ACL + 机器护栏锁死 | **CLOSED · 规格/静态** |
| P0-12 | 任意非空 `source_ref` 曾可进入 staging、release 与检索，文档“唯一主源”没有机器执行力；旧来源还可能经 rollback 复活 | 26/33/39/OpenAPI 的旧自由文本合同 | CR-004 改为服务端批准的不可变 source version + 每 release 四域 binding；导入、完整候选发布、目标回滚、检索和离线 lease 均 fail-closed；拒绝审计独立提交 | **CLOSED · 规格/静态；运行仍待 DEV-M0** |

## 4. 企业复核 P1 收口与残余执行门

### 4.1 本轮已关闭的静态 P1

| ID | 复核发现 | 收口证据 | 状态 |
|----|----------|----------|------|
| P1-C01 | 原始 parser/storage 异常可经 `last_error`、任意 worker JSON、自由 token 或伪 diagnostic_id 进入 API 可见 `error_report` | OpenAPI `ImportFailureReport` 按 CANCELLED / MAX_ATTEMPTS / worker failure 分支，有限 issue code 与 DB enum 集合相等；opaque diagnostic_id 只由 DB 随机 UUID 生成；33 的授权函数拒绝或重建公开对象，并以表级 `import_batch_error_report_shape` CHECK 做纵深防御 | **CLOSED · 规格/静态** |
| P1-C02 | claim→文件 I/O→finalize 若共用显式事务，会形成 outbox→batch 反向锁序 | 39 / 46 冻结 claim、heartbeat 各自独立短事务立即 COMMIT，claim 后才文件 I/O，retry/finalize 新事务；架构护栏锁死文字合同 | **CLOSED · 规格/静态；仍待 PG 死锁负测** |
| P1-C03 | `/ready` 未表达 import storage readiness | OpenAPI `ReadyChecks` 新增必填 storage；ready 五项全 ok，not_ready 至少一项为 not_ready；探针描述与 39/46 同步写/重读完整性探测 | **CLOSED · 机器合同/静态** |
| P1-C04 | 39 页头曾与 OpenAPI/DDL 版本谱系漂移 | 当前冻结为 OpenAPI 1.11.0、37 v1.16、39 v1.15、40 v1.18、schema v1.12、41 v1.13、43 v1.6、本报告 v1.38、46 v1.19、48 v0.5、50 v0.4；当前 schema reference DDL 本机 PG15 预检已 `PASS-WITH-LIMITATION`，运行与迁移证据边界未放宽 | **CLOSED · 文档谱系** |
| P1-C05 | 46 的 Ddev 硬门入口漏 Cost Owner，但后文 Owner 台账与 DoR 要求七类 Owner | 46 v1.4 在硬门入口显式列 Product/Tech/Security/Content/QA/Ops/Cost 七类，并增加结构护栏 | **CLOSED · 治理合同** |
| P1-C06 | 46 曾要求“§13 所有 P0 决策门 PASS”，但表中没有 P0 标记，且 DEV-M4/上线门会被错误前置到 DEV-M0 | 46 v1.4 改为按最晚时间条件触发：DEV-M0 只要求到期门；stage/commit/push 单独受 DEC-PUBLISH-01 约束；其余门不倒置 | **CLOSED · 阶段语义** |
| P1-C07 | DDL 证据曾误写为 fresh clone 可重跑，但 SQL 与 grammar test 当前均未进入 Git 基线 | 当前本地 workspace 可复跑；SQL 与 grammar test 仍是未跟踪候选，须批准并原子纳入 changeset 后才可由 fresh clone CI 复现 | **CLOSED · 证据分级** |
| P1-C08 | 44 曾同时承担历史评分与规范附录，造成权威链混合 | 有效合同已吸收到 37/39/41/47/49；44 全文统一降为历史评审证据，46 与静态护栏不再把 44 当实现真源 | **CLOSED · 权威链** |

### 4.2 未闭环，但不阻塞 Phase B 文档

| ID | 残余项 | 证据/影响 | 门禁 |
|----|--------|-----------|------|
| P1-01 | immutable migration、N/N-1、application runtime、托管 PG、并发/死锁与备份恢复仍未认证 | 当前 schema v1.12 reference DDL 本机隔离 PG15 预检已闭环，但 33 不是就地升级器，当前没有上一发布版本或 `db/migrations/000*.sql` 可供真实升级演练 | Ddev 后进入 DEV-M0，生成不可变迁移并跑 empty install、来源/内容治理负例、升级/backfill、最小权限与失败回滚；未绿不进入 DEV-M1 |
| P1-02 | 业务基线与运行交接方案已签，但 G0、Ddev 及权威来源 / 费用 / G0-14 WBS 与成本包仍未齐；真实环境、数据与运行负例仍待后续门取证 | 02 §2、05 §7.1、46 §1.2/§13/§18 | **阻塞一切业务代码、部署、试点** |
| P1-03 | 业务阈值尚未签：hit@3/no-hit、活动价有效期、上传上限/留存、指标、iteration-task SLA、工单字段 / 口径 / 保留期 | 46 `DEC-SEARCH/CONTENT/UPLOAD/METRICS/ITERATION-TASK/WORKORDER` | 对应 DEV 里程碑未过决策门不得标 Done |
| P1-04 | 未做真设备、投影、读屏与触控验收 | Chromium 多视口/Axe/交互自动化已绿，但不能替代硬件与辅助技术 | 图站正式评审前补人工/真机验收 |
| P1-05 | 当前 origin 为 Public，设计包标注 Proprietary/internal use | 本地未跟踪文件若 add/commit/push 会公开；未核准本地资料虽已精确本地排除，但静态脱敏测试仍不等于发布批准 | **P0 发布门 `DEC-PUBLISH-01`**：整个 changeset 的 exact-file manifest + 脱敏 + Product/Security/Tech Owner 书面批准 + license/private 决策；未签不 stage/commit/push |
| P1-06 | CR-004 四个真实主源、权限、质量分母、最终 EVD 与运行实现均未形成 | G0-09 仍进行中，Scope #9 未勾；当前 DDL/OpenAPI 静态机器合同虽已锁定，数据库迁移、服务端与客户端 runtime 均未实现 | G0 先补真实四源清单与 Content Lead EVD；Ddev 后 DEV-M0 实现 registry/binding/暂停/拒绝审计并做真 PG、API、离线 lease 负测，未绿不得宣称硬门可用 |

原 P1-08（现行文档与测试夹具漂移）在 v1.23 快照已闭环：当时修正版本/链接/私有边界夹具与业务 QA 造数，未放宽公开边界、未伪造 Ddev；该轮 `npm run test:all` 已通过。DEC-042 v1.26 与 v1.27 的生成与全量回归作为历史快照保留；v1.34 保留最近一次全量回归，v1.35 保留 G0-10 / Scope #10 状态快照，v1.36 保留 G0-11 / Scope #12 状态快照，v1.37 记录 G0-12 / Scope #13 状态增量的分层回归，见第 9 节；静态绿灯仍不替代 Ddev、真 PG 或真实客户端证据。

v1.26 又消除了一处 Ddev / DEV-M0 字面循环：OpenAPI 生成方向和 CR-004 静态合同按已有证据勾选；Ddev 前只保留 reference DDL 隔离预检，migration、生成类型、runtime、N/N-1 与动态负例统一放入 DEV-M0 Done。G0-08 和 G0-10 仅更新为“仓内可签包已形成、受控 EVD 待签”，不改变 3/14、3/15、Ddev 或开发状态。

v1.27 将 13 角色具名与接受统一到 `02` §5 的唯一 RACI 入口，并把 `DEC-DDEV-01` 从文档模板提升为独立机器授权硬门；冻结输入新增对 37 v1.15、46 v1.17 的现行真源精确比较，并拒绝 `010` 冒充 `01`。本次只收紧授权判断，不改变 G0 `3/14`、Scope `3/15`、Ddev 为空或代码未开始的状态。

v1.28 登记当前 schema v1.12 reference DDL 本机隔离 PostgreSQL 15.18 预检 `PASS-WITH-LIMITATION`；只关闭 Ddev 前 reference DDL 前置形状验证，不将本地 ignored 证据冒充不可变迁移、N/N-1、应用运行态、托管库、备份恢复、并发/死锁或生产证据。G0 `3/14`、Scope `3/15`、Ddev 为空、Gate 与代码状态不变。

v1.29 补齐 G0-14 单人 FDE WBS、B 路径成本算式与 G0-15 运行交接可签草案，并将 G0-08 / G0-10 的 Gate/Scope 成对推进、G0-14 / G0-15 同时通过才能勾选 Scope #15、项目负责人 RACI 接受证据写成机器门。这些只把草案推到“可签”，不代替真实容量、预算、值守或 Owner EVD；G0 `3/14`、Scope `3/15`、Ddev 为空、Gate 与代码状态不变。

v1.32 登记 `EVD-RACI-ACCEPTANCE-PACK-20260810`：13 个角色职责接受完成，原子关闭 G0-04、Scope #2 与 Scope #4，当前为 G0 `4/14`、Scope `5/15`、合计 `9/29`；四类核心主责与代理共八个代号提前执行全局分离。该包不替代业务基线、费用、安全、运维、WBS 与运行交接等事项专用签发，G0 仍未签、DEC-DDEV-01 仍 `PREPARED`、Ddev 为空、代码未开始。

### 4.3 文档发布复核收口（2026-08-07）

| ID | 独立复核发现 | 本轮处理 | 状态 |
|----|--------------|----------|------|
| P1-DOC-01 | 历史 QA 产物被 Git 忽略，fresh clone 不可取得；60 分钟封口目录另缺 3 个预期产物 | 三份历史评审补充本机/忽略/fresh-clone 边界；逐项列出缺失产物；禁止用当前回归补造历史证据 | **CLOSED · 治理边界；历史完整复算仍不可用** |
| P1-DOC-02 | `DEC-PUBLISH-01` 只锁路径，没有锁定候选内容 | 确定性生成器现已锁定完整工作树路径/状态、M/A 原始字节哈希、Git file mode、Base HEAD/branch/origin 与本地排除处置；三方获批后还须用同一 bundle 复核 staged index blob/mode；真实临时 Git 仓已覆盖精确暂存成功及 blob、mode、额外路径三类篡改拒绝 | **CLOSED · 工具/静态；三方发布审批仍 OPEN** |
| P1-DOC-03 | 公共站 README 把 PRE-D0 会议快照写成“当前会议视图” | 改为“PRE-D0 / 需求阶段会议快照 · 非当前设计 SSOT” | **CLOSED** |
| P2-DOC-04 | 公共站 README 的架构合同计数落后 | v1.23 / v1.26～v1.33 仅作历史快照；v1.34 全量基线与 v1.37 状态增量均实跑 37/37 PASS | **CLOSED** |
| P2-DOC-05 | 8 个删除项被误写为全部与设计归档配对 | 校准为 7 个设计旧路径配对归档；`temp/README.md` 是独立清理 | **CLOSED** |
| P2-DOC-06 | 2026-07-31 归档入口的显示文本比真实相对路径少一层 | 改用语义标签，链接目标保持正确 | **CLOSED** |
| P2-DOC-07 | 现行设计文档裸引历史 `34/36/38/42`，易被误读为现行 SSOT | 改为带“历史”标签的归档真路径链接 | **CLOSED** |

## 5. P2 清单

计数口径：44 的 `P0/P1/P2=0/0/0` 只针对**本轮静态 OSS 工程审计**；Ddev 后才到期的 runtime 版本锁属于未来执行门，不计入当前静态 OSS P2。

| ID | 项 | 处理 |
|----|----|------|
| P2-01 | 业务 runtime 的 Fastify/pg/Electron 精确 patch 尚未锁 | **DEFERRED · Ddev 执行门，不计入当前静态 OSS P2。** 架构工具链已锁 Node 24、Redocly/PlantUML；业务 runtime 须在 Ddev 后 DEV-M0 建 package/lock，禁止 floating latest |
| P2-02 | 初始 PRD 曾把 RAG/DeepSeek 写得像一期主路，且工单方向与信息架构未按 CR-001 收口 | 25/29/31/32 已改为 bigram 主路、DeepSeek 默认关；UC-04 明确一期只读工单分析，与内部话术优化待办分域；**CLOSED** |
| P2-03 | 图站文案与 43 曾漏 Tab7/8766/异步边 | 40/43/HTML/PlantUML 源已修，本地确定性生成器已重建并通过 7/7 check；**CLOSED** |
| P2-04 | 部分 Markdown 用双空格表达硬换行，`git diff --check` 曾报 trailing whitespace | 已机械改为 CommonMark 反斜杠硬换行并复跑 `git diff --check`；**CLOSED** |
| P2-05 | 44 曾混合规范性附录和历史评分 | 有效合同吸收到 37/39/41/47/49；44 全文统一为历史评审证据；**CLOSED** |

## 6. 与“设计 10 分”口径的差异

1. **不重开架构路线。** 架构拓扑、SoR、九端口和产品红线仍成立。
2. **初始 16/16 绿不等于规格无洞。** 它当时主要检查关键词存在，没有覆盖 44 §3、PUBLIC EXECUTE、owner-only、非破坏迁移、rollback、双域语义与 fencing。
3. **修补后也不自签 10。** 静态护栏、Redocly、SQL parser、PlantUML、Chromium 自动化与当前 schema v1.12 reference DDL 本机 PG15 预检提高了可追溯性，但不可变迁移、N/N-1 与上一发布版本升级、CR-004 / DEC-042 应用运行硬门仍为 `NOT_CERTIFIED / NOT_IMPLEMENTED`；托管 PG、来源暂停传播、并发/死锁、备份恢复、生产环境、OAuth、产品 E2E、压测和安全测试是后续独立关卡。

## 7. 本轮 diff 摘要

| 文件 | 变更 |
|------|------|
| `25` / `29` / `31` / `32` | 一期检索改回 Unicode bigram 主路；DeepSeek/LLM 只可选排序；CR-001 业务工单分析进入 Dashboard；与内部话术优化待办分域；冻结 Windows / OAuth / 20+12+18 边界 |
| `37` / `39` / `41` / `43` | 九端口、危险 flag 硬禁、owner-only、新 release 回滚、内容/工单双域幂等与 outbox fencing、AUTH_MODE 单真源、中文 FTS、输入预算与 SLI 对齐；新增 N/N-1 兼容周期、只读 PlatformAdapter 与 CR-004 权威来源硬门；44 已迁为 90 下历史快照 |
| `33` | publish/rollback/import/cancel、withdraw MERGE、HMAC 版本、幂等、2-gram 权重、能力角色、单调 ACK、`work_order_*` / `iteration_task*` 独立表与 ACL、两类 import 耗尽闭环；CR-004 四域来源围栏；DEC-042 受控 search、Question 不可变版本、固定角色异人双审、审核决定与质量 plan/evidence 分账；补质量 population manifest、semantic asset 单向退役、封闭 public mapper 与 workload-specific denial wrapper |
| `39` / `openapi.v1.yaml` | 27 条 `/v1` 路径 + 2 探针；CR-002 自动事实与双账；CR-004 source bindings/拒绝 reason/source binding hash/短 lease；DEC-042 public wire 只暴露业务所需字段，不暴露审核主体 ID；runtime/admin denial wrapper 分账；不新增在线复核或训练 API |
| `40` / `43` / `diagrams` / HTML | 7 Tab 映射、生产 OAuth、Node 24、Owner 发布、持久 outbox + TS worker、CR-004 来源绑定时序、ARIA/键盘/contain fit、8766 静态服务提示；PlantUML 拓扑不新增组件或端口；7/7 确定性生成链保持对齐 |
| `00`～`05` | `EVD-G0-07-FEE-PATH-20260813` 与 `EVD-G0-14-WBS-CAPACITY-20260813` 已关闭 G0-07 / G0-14 及 Scope #11 / #15；当前为 13/14、14/15、27/29。仅 G0-09 / Scope #9 待关闭；G0、Ddev、真实 Pilot / G1b 与运行证据仍未授权 |
| 生命周期入口 / 历史 / 参考 | 现行设计、已完成 D0 证据、PRE-D0 会议快照和 99 历史设计包分层；旧标题 / 旧状态不再承担当前 Scope |
| `tests` / `sites package` | v1.23 / v1.26～v1.33 仅作历史快照；v1.34 保留全量基线，v1.37 状态增量实跑 **37/37 PASS**，继续锁定 DEC-042、denial wrapper ACL、13 角色接受包、G0-08 / G0-10 / G0-11 / G0-12 证据、八代号职责分离、RACI↔Gate/Scope 双向原子推进、DEC-DDEV 授权与 DEC-PUBLISH exact-file/index 复核；不弱化 public tracked 边界 |
| `46` | Phase B 开工包对齐九端口、DEV-M0–M4、两类工单工作流、ENV、检索、安全、Owner/决策门与 Ban；新增 Additive/Transition/Breaking/ACL 迁移兼容矩阵与首版无 N-1 的诚实 N/A 规则 |
| `sites/tests` / `08-工具` | 建立 13 角色唯一 RACI、人员与代理、接受证据、生效日期、职责分离和七类 Owner 投影校验；`DEC-DDEV-01` 精确校验状态、G0 / Scope / EVD、01 / 03 / 04 / 37 / 46 现行版本、环境与数据、费用、日期及四类独立签发人；拒绝近似状态词、陈旧 37 / 46 版本和 `010` 冒充 `01`；DEC-PUBLISH 在真实临时 Git index 中验证精确暂存，并拒绝 blob、mode 与额外 staged 路径篡改 |
| `sites/package.json` / `sites/package-lock.json` | 增锁定 SQL parser 与 grammar gate；lockfile 保留 npmjs / npmmirror 两类历史 resolved 源并以 integrity 锁定，不声称已统一 registry；不改业务代码 |

## 8. 下一步 3 个文件级动作

1. **先关闭唯一剩余组织门，不写业务代码：** 补齐 G0-09 / Scope #9 的四域权威来源整体签发，再完成正式 G0、`DEC-DDEV-01` 与 `DEC-PUBLISH-01`。
2. **Ddev 后进入 DEV-M0：** 以 OpenAPI 1.11.0 为输入生成 TS 类型并初始化 `apps/api`；同时从 schema v1.12 生成不可变迁移，建立预期 N-only（后续 N-1）合同夹具、PlatformAdapter、CR-004 来源硬门与 DEC-042 内容治理负例。真实 N 只在首个签名 Pilot 发布时建立，不从 DEV-M0 提前启动 90 日时钟。
3. **把兼容、来源与内容治理硬门设为 DEV-M0 退出门：** 在 empty PG15、来源未登记/暂停/域错/hash错/四域不全、受控 search ACL、Question 版本不可变、双审异人、质量 plan/evidence capability、并发发布/回滚、短 lease 断网、存在签名基线后的 N-1 升级、托管 PG 与最小权限身份路径实跑；首版无 N-1 时记 N/A，不得伪造；未过 OpenAPI/PG/ACL/source/content gate 不注册业务路由、不退出 DEV-M0、不进 DEV-M1。

## 9. 回归状态（v1.23 / v1.26～v1.40 为历史快照；v1.34 为最近一次全量基线；v1.41 为当前状态增量回归）

| 检查 | 结果 |
|------|------|
| `tests/test_arch_ssot_invariants.py` | **v1.41 PASS · 37/37**；锁定 13/14、14/15、27/29、费用 / WBS 精确 EVD，以及 G0 / Ddev 不变 |
| `node --test customer-project-status + contract` | **v1.41 PASS · 37/37**；状态派生、正式 B、精确 EVD、成对门、Ddev 精确版本与文档契约通过 |
| `npm run lint:openapi` | **v1.34 PASS（历史全量基线）** · v1.37 未改 OpenAPI，未重复运行；OpenAPI 3.1.1 / API 1.11.0，29 paths / 29 operations；SHA-256=`0dba20e22a7bba5a5bf93ad7383e42be97b0da3f7b5953860ae226823af016be` |
| `npm run check:arch-diagrams` | **v1.41 PASS · 7/7** · PlantUML 源、SVG 生成物与 HTML 内嵌图一致 |
| `npm run test:arch-board-ui` | **v1.37 PASS** · 4 视口 × 7 Tab、Axe 0、键盘/缩放/可读层通过 |
| `npm run test:sql-grammar` | **v1.34 PASS（历史全量基线）** · v1.37 未改 SQL / grammar，未重复运行；513 SQL statements + 89 function bodies + 20 DEC-042 guards |
| `sync_customer_agent_surfaces.mjs --check` | **v1.41 PASS** · `07/08` 为 7/7 真源稳定点；`09` 冻结 SHA `29eebbc7…d9d5`、mtime `1786205613` 均未改变 |
| `npm run test:business` | **v1.37 PASS（最近一次业务浏览器基线）** · v1.38 未重复运行；PRD 49/49、执行中心 57/57、需求会冻结快照 30/30 |
| `node --test public-project-boundary` | **v1.41 PASS · 8/8**；当前公开基线 13/14、14/15、27/29，仓外私有合成夹具仅推进 G0-09 / Scope #9 到 14/14、15/15、29/29 |
| 全库 Markdown 本地链接 | **v1.37 PASS · 缺失 0**；覆盖当前候选投影中的 Markdown / HTML 本地链接，不冻结易随文档增量漂移的链接总数 |
| 公开边界与敏感路径回归 | **v1.37 PASS · 8/8**；仍不替代 Security Owner 对整个 changeset 的发布批准 |
| `sites/npm run test:all` | **v1.34 PASS（最近一次全量基线）** · 核心 Node 128/128、Python 37/37、通用 UI、架构看板、业务 QA、OpenAPI、SQL、七图与依赖审计当时全绿；**v1.37 按文档状态变更采用分层回归，未重复运行整条命令** |
| `npm run test:docs:fast` | **v1.41 PASS** · 当前文档差异与暂存区空白检查通过 |
| `git diff --check` | **v1.41 PASS** · tracked 与 cached 均无空白错误；未暂存、未提交、未推送 |
| 本机 PG15 设计前置验证 | **v1.34 PASS-WITH-LIMITATION（历史且仍适用的 current reference DDL preflight）**；v1.37 未改 schema，不重复运行；`EVD-PG15-LOCAL-PREFLIGHT-20260810T055834+0800-ED5F9822` 不替代 migration / N/N-1 / runtime / managed PG / backup-restore / concurrency-deadlock / production 证据 |
| 真设备与读屏 | **未执行**，见 P1-04 |

```bash
cd "$(git rev-parse --show-toplevel)"
PYTHONDONTWRITEBYTECODE=1 python3 business-docs/01-客服Agent项目/20-设计-进行中/tests/test_arch_ssot_invariants.py
# v1.37：summary fail=0 total=37
```

## 10. DEC-PUBLISH-01 重新生成规则（当前仅有本地候选，尚未批准）

> **当前状态：** **BLOCKED / CANDIDATE_NOT_APPROVED**。仓内已有确定性生成器，但本报告是 90-评审中的非规范证据，**当前不声明精确路径数，也不内嵌当前摘要**；权威候选只生成到 ignored `output/customer-agent-publish-gate/`，审批原文只在公司受控系统保存。任何旧路径数、旧状态或旧摘要均不得解释为当前工作树状态，也不得据此暂存或审批。

规范规则以 `../20-设计-进行中/46-实现设计-开工包.md` 的 `DEC-PUBLISH-01` 与 `../../08-工具/prepare_customer_agent_publish_manifest.mjs` 为准：

1. 生成前强制 index 为空、无冲突；双采样完整非忽略工作树，rename 展平为旧路径 `D` + 新路径 `A`。
2. `workspace-inventory.tsv` 覆盖全部变化并标 disposition，同时记录 HEAD/index 相关 file mode；`stage-manifest.tsv` 对每个 INCLUDE 项编码 `<status>\t<path>\t<sha256-or->\n`，按 UTF-8 路径字节序排列。当前工具不接受静默排除：若要拆分，先整理工作树再重生成。
3. `M/A` 一律使用工作树原始字节 SHA-256，`D` 使用 `-`；bundle SHA 只留 ignored 候选和受控 EVD，禁止回写本报告或任何候选内文件，因此不存在自引用归一化。
4. bundle 同时绑定 Base HEAD、branch、脱敏 origin、全量 inventory、stage manifest、本地排除类别及各自摘要；符号链接、执行位、未知二进制、私密/ignored 路径、单边历史迁移、缺失生成配对或高置信凭据一律失败关闭。
5. Product Owner、Security Owner、Tech Owner 必须对**同一个 bundle SHA**分别确认业务范围、脱敏/公开边界、Proprietary/license、代码与生成物一致性，并分别留下 `EVD-*`。
6. 任一路径、状态、内容、HEAD、branch、origin、disposition 或排除处置变化，清单、单文件哈希和三方审批全部失效，必须重新生成。
7. 三方均 PASS 后也只能按 `stage-manifest.tsv` 精确暂存；禁止 `git add .`。暂存后、commit 前必须以同一 bundle 运行 `--verify-staged`，要求 index 路径/状态/blob/mode 精确一致、无剩余 unstaged/untracked 且生成稳定点未漂移。工具返回 `STAGED_MATCH` 仍不等于自动获得 commit/push 授权。

复核命令：

```bash
npm --prefix sites run prepare:customer-agent-publish-manifest
# 审批人按候选 README 中的 bundle SHA 复核：
node business-docs/08-工具/prepare_customer_agent_publish_manifest.mjs --expect-bundle=<64位bundle-sha>
# 仅在三方批准且人工精确暂存后；当前未获批，所以本轮不会执行：
node business-docs/08-工具/prepare_customer_agent_publish_manifest.mjs --verify-staged=<64位bundle-sha>
```

## 11. 修订

| 版本 | 日期 | 说明 |
|------|------|------|
| v1.41 | 2026-08-13 | 归档 `EVD-G0-07-FEE-PATH-20260813`：正式选择 B 路径、开发阶段新增付费 0、复用既有人员 / 设备 / 本地 PostgreSQL、2026-08-31 再决策；与 `EVD-G0-14-WBS-CAPACITY-20260813` 原子关闭 G0-07 / G0-14 及 Scope #11 / #15，当前为 G0 13/14、Scope 14/15、合计 27/29。定向回归 Node 状态+契约 37/37、公开边界 8/8、Python 37/37、七图 7/7、业务真源 7/7；仅 G0-09 / Scope #9 仍开放，G0 / Ddev 未授权，未 stage/commit/push |
| v1.40 | 2026-08-12 | 归档 `EVD-G0-03-BUSINESS-BASELINE-20260812`，原子关闭 G0-03 / Scope #5 / #6，当前为 G0 11/14、Scope 12/15、合计 23/29；产品、活动、VOC 来源实际快照计数与早期预填假设分账，假设值不冒充正式基线。分层回归实测 Node 状态+契约 37/37、公开边界 8/8、Python 37/37、七图 7/7、业务真源 7/7、docs fast PASS；09 冻结 SHA / mtime 保持不变；未重复运行浏览器、SQL、OpenAPI、依赖审计与整条 `test:all`，未 stage/commit/push |
| v1.39 | 2026-08-12 | 归档 `EVD-G0-13-EVALUATION-FREEZE-20260812`：`G1A-DAFUYAN-PREFILL-20260812` 共 50 条，第一轮 `USR-TIANYUAN-001` 与第二轮 `USR-JIANGHUA-001` 均确认 Pass，原子关闭 G0-13 / Scope #14，当前为 G0 10/14、Scope 10/15、合计 20/29；50 条冻结包不冒充真实 Pilot / G1b，G0-14 WBS / 成本包、费用与 G0-09 来源质量仍开放，未 stage/commit/push |
| v1.38 | 2026-08-12 | 归档 `EVD-G0-15-RUN-HANDOVER-20260812`，单独关闭 G0-15，当前为 G0 9/14、Scope 9/15、合计 18/29；Scope #15 因 G0-14 WBS / 成本包未 Pass 继续不勾。该证据只批准运行与交接方案，不冒充真实告警、恢复演练、Pilot、G0 或 Ddev；费用保持后置，未 stage/commit/push |
| v1.37 | 2026-08-10 | 归档 `EVD-G0-12-OPS-DEPLOYMENT-20260810`，原子关闭 G0-12 / Scope #13，当前为 G0 8/14、Scope 9/15、合计 17/29；冻结未来受控测试环境的 single_host、PostgreSQL 15.18、development / test mock 与 Pilot 真 OAuth + 服务端 RBAC 方案，但不冒充环境、runtime、备份恢复或 Pilot 证据。同步 00/02/03、07/08、架构状态、图源/生成物与定向测试，09 冻结 SHA / mtime / size 保持不变；分层回归实测 Node 状态+契约 37/37、业务 QA 49/49 + 57/57 + 30/30、公开边界 8/8、Python 37/37、七图 7/7、四视口 UI 全绿；未重复运行 SQL/OpenAPI/依赖审计与整条 `test:all`。费用按用户指示暂不推进，G0、Ddev、DEC-PUBLISH-01 仍开放，未 stage/commit/push |
| v1.36 | 2026-08-10 | 归档 `EVD-G0-11-SECURITY-BOUNDARY-20260810`，原子关闭 G0-11 / Scope #12，当前为 G0 7/14、Scope 8/15、合计 15/29；批准一期 0 / 14 / 30、外部模型 / 训练 NO-GO、最小权限、日志、语义资产晋级与删除血缘策略，但真实数据逐批双审、运行负例、备份恢复、训练、Pilot 与 Ddev 仍未授权。同步 00/02/03、07/08、架构状态、图源/生成物与定向测试，09 冻结 SHA / mtime / size 保持不变；分层回归实测 Node 状态+契约 37/37、业务 QA 49/49 + 57/57 + 30/30、公开边界 8/8、Python 37/37、七图 7/7、四视口 UI 全绿；未重复运行 SQL/OpenAPI/依赖审计与整条 `test:all`。费用按用户指示暂不推进，G0、Ddev、DEC-PUBLISH-01 仍开放，未 stage/commit/push |
| v1.35 | 2026-08-10 | 归档 `EVD-G0-10-PRD-SCOPE-FREEZE-20260810`，原子关闭 G0-10 / Scope #10，当前为 G0 6/14、Scope 7/15、合计 13/29；同步 00/02/03、07/08、架构状态、图源/生成物与定向测试，09 冻结 SHA / mtime / size 保持不变；分层回归实测 Node 状态+契约 37/37、业务 QA 49/49 + 57/57 + 30/30、公开边界 8/8、Python 37/37、七图 7/7、四视口 UI 全绿；未重复运行 SQL/OpenAPI/依赖审计与整条 `test:all`。费用按用户指示暂不推进，G0、Ddev、DEC-PUBLISH-01 仍开放，未 stage/commit/push |
| v1.34 | 2026-08-10 | DEC-PUBLISH-01 增加真实临时 Git index 端到端回归：精确 staged 候选通过，blob 内容、file mode 与额外 staged 路径篡改均失败关闭；生产 CLI 仍固定当前仓库与完整生成检查，无跳过检查参数；全量回归重跑整条命令 exit 0，核心 Node 128/128、Python 37/37、业务 QA 49/49 + 57/57 + 30/30、七图、四视口 UI、OpenAPI、SQL 与依赖审计全绿；费用、G0、Scope、Ddev 与代码状态不变，候选仍未批准，未 stage/commit/push |
| v1.33 | 2026-08-10 | 归档 `EVD-G0-08-GREENFIELD-ISOLATION-20260810`，原子关闭 G0-08 与 Scope #8，当前为 G0 5/14、Scope 6/15、合计 11/29；同步 00～08、架构状态、图源/生成物与机器门，09 冻结 SHA / mtime / size 保持不变；全量回归核心 Node 123/123、Python 37/37、业务 QA 49/49 + 57/57 + 30/30、七图、四视口 UI、OpenAPI、SQL 与依赖审计全绿；费用按用户指示暂不推进，G0、Ddev、DEC-PUBLISH-01 仍开放，未 stage/commit/push |
| v1.32 | 2026-08-10 | 登记 `EVD-RACI-ACCEPTANCE-PACK-20260810` 的 13 角色职责接受；原子关闭 G0-04、Scope #2 / #4，当前为 G0 4/14、Scope 5/15、合计 9/29；将四类核心主责 / 代理八代号分离、RACI↔Gate/Scope 双向一致与私有工作区前进一步计数纳入机器门；07/08 与架构图重新生成，09 冻结字节 / mtime 不变；全量回归核心 Node 123/123、Python 37/37 及其余门禁全绿；各事项专用 EVD、G0、Ddev 与 DEC-PUBLISH-01 仍开放，未 stage/commit/push |
| v1.31 | 2026-08-10 | DEC-PUBLISH-01 增加 staged index 精确复核：inventory 原始字节成为 mode 唯一真源并与 JSON/stage manifest 交叉校验；候选五文件逐项 no-follow，生成与 staged 复核均采用检查前后双采样；完整提交投影扫描 Markdown/HTML，新增 Python/脚本文本卫生范围、清单排序/重复/hash、37/46→07/08、PUML 同名配对、候选 README 及符号链接负例；全量回归核心 Node 123/123、Python 37/37 及其余门禁全绿；候选仍未批准，未 stage/commit/push，G0/Scope/Ddev/Gate4 不变 |
| v1.30 | 2026-08-10 | 新增 DEC-PUBLISH-01 双层候选生成与机器门：完整 workspace inventory、精确 stage manifest、HEAD/branch/origin/双摘要绑定、迁移与生成物原子配对、敏感/符号链接/二进制/执行位失败关闭及三方同摘要复核；候选仍未批准，未 stage/commit/push，G0/Scope/Ddev/Gate4 不变 |
| v1.29 | 2026-08-10 | 补齐 G0-14 单人 FDE WBS、B 路径成本算式与 G0-15 运行交接可签草案；按现有 Mac + Windows 不升级、未来云资源待批的边界记账；新增 G0-08 / G0-10 Gate/Scope 成对门、G0-14 / G0-15 共同关闭 Scope #15 与项目负责人 RACI 证据门；全量回归实测核心 Node 113/113、Python 37/37 及其余门禁全绿；G0 3/14、Scope 3/15、Ddev 为空、Gate 与代码状态不变 |
| v1.28 | 2026-08-10 | 登记当前 schema v1.12 reference DDL 本机隔离 PostgreSQL 15.18 预检 `PASS-WITH-LIMITATION`，锁定最终 SHA / EVD / 40 tables / 2 views / 143 functions / ACL 8/8 / constraints 3/3 / 幂等 / 原子回滚证据；明确本地 ignored 证据与 fresh clone 重跑边界；migration / N/N-1 / runtime / managed PG / backup-restore / concurrency-deadlock / production 仍未认证；G0 3/14、Scope 3/15、Ddev、Gate 与代码状态不变 |
| v1.27 | 2026-08-10 | 将 13 角色具名与接受收敛到 `02` §5 唯一 RACI 入口，固定职责分离与七类 Owner 投影；新增 `DEC-DDEV-01` 独立机器授权硬门，精确核验 G0 / Scope / EVD、01 / 03 / 04 / 37 / 46 现行版本、环境与数据、费用、日期及四类独立签发；拒绝近似状态词、陈旧 37 / 46 版本和 `010` 冒充 `01`；G0 3/14、Scope 3/15、Ddev 为空、代码未开始均不变；全量回归实测核心 Node 111/111、Python 37/37，其余门禁全绿 |
| v1.26 | 2026-08-10 | 消除 Ddev / DEV-M0 循环，将静态 reference DDL 预检与 Ddev 后 migration/runtime 退出证据分账；如实勾选 OpenAPI/CR-004 静态完成项；G0-08 和 G0-10 仓内可签包已形成但受控 EVD 待签，计数、Ddev 与开发状态不变；生成与全量回归重跑通过 |
| v1.25 | 2026-08-10 | 对齐 DEC-042 postfix 最终静态机器基线：schema v1.12 / OpenAPI 1.11.0、最终三份 SHA、513/89/20 grammar 形状与 29/29 API；补 population manifest、semantic asset 单向退役、封闭 public mapper 与 workload-specific denial wrapper；最终生成及全量回归已通过；Gate / G0 / Scope / Ddev 不变 |
| v1.24 | 2026-08-10 | 对齐 DEC-042 最终静态机器基线：schema v1.11 / OpenAPI 1.10.0 与双 SHA、452/76/9 grammar 形状；补受控 search、Question 版本、异人双审及质量证据信任边界；将 v1.23 测试结果明确降为历史快照，最终全量回归待补；Gate / G0 / Scope / Ddev 不变 |

---

**签字式结论：** 设计与实现设计文档已可交给工程团队；当前没有开放的规格级 P0，但 **DEC-PUBLISH-01 仍是 P0 发布门，Ddev 仍未授权**，所以本结论是“开工包就绪”，不是“可暂存/提交/推送”，也不是“代码已可开发/上线”。
