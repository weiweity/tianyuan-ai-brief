# G0-09 只读复核记录

> **记录性质：** `READ-ONLY REVIEW` · 不构成 `EVD-*`、不改变 G0-09 / Scope #9 状态、不授权 G0 / Ddev。
>
> **首轮复核日期：** 2026-08-26 · **补记截至：** 2026-08-28 · **复核视角：** 当前 lark-cli `user` 身份

## 1. 复核范围

本轮只核对当前飞书登录态、可见资源召回和权限设置接口能否形成新鲜、可归属的 G0-09 证据。没有执行权限修改、文档创建、上传、覆盖、成员变更或项目状态写入。

## 2. 机器读侧结果

| 检查项 | 结果 | 能否作为 G0-09 关闭证据 |
| --- | --- | --- |
| 当前 CLI 用户登录态 | `verified=true`、用户 token 有效 | 否；只证明认证成功 |
| MENOKIN 相关表的搜索召回 | 可见两份 MENOKIN 相关表资源 | 否；搜索可见不等于有权限读取自身 ACL |
| 已召回 Wiki/Sheet 资源的权限设置读取 | 两个目标均返回 `permission_denied`（当前用户缺少目标权限） | 否；不能据此推断组织外访问已关闭或仍开放 |
| 截图中的售前对象 URL 检视 | 可解析为通用 `file`，标题为空 | 否；无法与 `presale` 的现行主源、版本快照建立公开安全映射 |
| 该通用 `file` 的权限设置读取 | 接口返回 `invalid_parameters` | 否；当前 shortcut 没有形成可验证的权限字段结果 |

## 3. 状态处理

本轮没有取得可以写入公开台账的四域整体收据，也没有取得售前 / 售后候选文件的稳定 `source_ref`、`srcv_*`、现行 ACL、质量分母和 Content Lead 最终签发。因此继续保持：

- G0-09：`进行中`；
- Scope #9：未勾选；
- 外部责任包：`13/14`；
- Scope：`14/15`；
- G0：未签发；
- Ddev：未授权。

## 4. 可继续推进的解阻动作

需要由能实际管理这两个候选对象的独立受控管理员 / 平台管理员提供新的脱敏受控证据包，至少包含：

1. 售前、活动、售后、产品四域各自唯一 `canonical / current` 主源与稳定版本快照；
2. 四域现行 ACL 证据，至少能核对外部访问、链接分享、继承关系和直接授权范围；
3. 四域质量盘点的 `total_rows / importable_rows / quarantined_rows` 分母；
4. 同一 `EVD-G0-09-AUTHORITY-SOURCES-YYYYMMDD` 整体签发收据，并将四行 `overall_approval_evd` 对齐到该收据；
5. 售前 / 售后候选对象的受控映射，避免用同名或相似 MENOKIN 表替代目标文件。

受控管理员完成后，先把脱敏字段回填到公开安全投影，再运行 G0-09 / Scope #9 的机器校验；在此之前不得修改计数或宣称权限整改已验证。

## 5. 公司副本 `客服agent` 的受控复核

本轮在当前公司账号的“我的空间”创建 `客服agent` 表格副本，用于后续客服项目协作；复制操作未对原始《MENOKIN话术》执行写入。以下只记录脱敏后的控制结果，不保存 URL、token、原始快照或成员清单。

| 检查项 | 新鲜结果 | 证据边界 |
| --- | --- | --- |
| 副本归属与类型 | 当前企业用户身份可见；类型为 `sheet` | 证明副本已落在公司账号空间，不证明其已被 Content Lead 指定为四域主源 |
| 密级 | 设置接口成功；页面重新读取显示 `L1`，且不再显示“本文档尚未设置密级” | 仅证明该副本密级已设置；不推断原始文档或其他来源的密级 |
| 组织外访问 | `external_access_entity=closed` | 当前副本的权限读取结果 |
| 组织内链接 | `link_share_entity=tenant_readable`、`share_entity=same_tenant` | 保留组织内只读协作，不等于匿名可访问 |
| 复制 / 打印 / 下载 | `copy_entity=only_full_access`、`security_entity=only_full_access` | 当前副本的权限读取结果 |
| 评论与协作者管理 | `comment_entity=anyone_can_view`、`manage_collaborator_entity=collaborator_can_view` | 现行设置仍按接口返回记录，不扩大为管理员专属 |
| 外部邀请 | 写入响应接受 `invite_external=false`，但权限读取接口未回显该字段；页面也没有独立同名项 | 不作为独立闭环 EVD；仍需管理员侧受控证据补齐 |

该副本只能作为“公司空间副本已建立、密级与当前权限已复核”的局部证据，不能替代售前、活动、售后、产品四域的唯一 `canonical / current` 主源、稳定版本快照、质量分母、完整 ACL EVD 或 Content Lead 最终签发。因此 G0-09、Scope #9、G0 与 Ddev 状态均不变。

## 6. 四域候选源的当前账号可见性复核

使用当前公司账号按四域相关关键词进行只读搜索，均能召回候选表；但对四个候选对象逐一读取自身权限设置时，全部返回 `permission_denied`（错误码 `1063002`）。本轮没有继续抓取正文、版本或成员信息，也没有根据标题相似度把候选对象映射到 `presale / campaign / aftersale / product`。

| 检查项 | 结果 | 结论 |
| --- | --- | --- |
| 搜索召回 | 四域均有候选对象可见 | 搜索可见不等于可核验、可登记或已获主源批准 |
| 文件自身 ACL 读取 | 4/4 `permission_denied` | 当前账号无法形成现行 ACL EVD |
| 版本 / 快照读取 | 未执行 | 无法生成 `srcv_*`、快照或质量分母 |
| Content Lead 主源指定 | 未取得 | 不得填写 `canonical / current` |

因此当前阻塞点已明确为：需要候选文件所有者或独立受控管理员向当前公司账号授予最小只读核验权限，或在公司受控系统提供脱敏 ACL、版本、快照和 Content Lead 签发证据。搜索权限本身不足以解除 G0-09；状态、计数和 Ddev 保持不变。

## 7. `客服agent` 的后续主源定位

用户已明确：本轮创建的公司副本将作为后续客服项目的主要维护入口。只读结构复核结果如下：

| 工作表 | 结构结果 |
| --- | --- |
| 售前 | 存在，普通网格表 |
| 售后 | 存在，普通网格表 |
| 活动 | 存在，普通网格表 |
| 产品话术 | 存在，普通网格表 |

因此将该副本登记为“四域主源候选 / `PRECONFIRM`”，后续可在同一工作簿内完成四域版本冻结与维护。此定位仍不等于四域已被 Content Lead 指定为 `canonical / current`，也不自动替代现有产品 / 活动来源；在完成唯一主源指定、版本快照、适用范围、ACL EVD、质量分母和最终批准前，禁止把它写入 G0-09 关闭收据或增加通过计数。

当前读取到的工作簿 revision 为 `0`；该版本号只能作为后续受控快照的起点，快照文件与 SHA-256 必须保存在公司受控系统，不写入本仓。

## 8. 四域结构与敏感格式初筛

使用 `+table-get` 对公司副本做一次性只读结构 / 聚合扫描，未保存正文。返回的 used range 均已记录；下表中的“敏感格式命中”只表示正则初筛命中，不判断是否为真实个人信息或业务热线，也不等同于质量隔离结论。

| 工作表 | used range | 物理数据行 | 非空行 | 空行 | 整行重复 | 敏感格式命中 | 结构备注 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| 售前 | `A1:C82` | 81 | 79 | 2 | 2 | 0 | 第三列为 `col3` 占位列，实际无值 |
| 售后 | `A1:C224` | 223 | 223 | 0 | 0 | 3 | 第三列为 `col3` 占位列，手机号格式初筛命中 3 行 |
| 活动 | `A1:B5` | 4 | 4 | 0 | 0 | 0 | 两列均有值 |
| 产品话术 | `A1:C107` | 106 | 106 | 0 | 0 | 0 | 首列表头为空白，但该列有值，需确认其业务含义 |

合计为 414 条物理数据行、412 条非空行。当前仍不能据此填写 `total_rows / importable_rows / quarantined_rows` 的最终质量收据：空行、整行重复、手机号命中、占位列和空表头都需要由内容 Owner / Content Lead 判断是可清理结构、合法业务内容还是必须隔离的问题；还未执行冲突、过期、适用范围和占位符规则的业务语义审查。

下一动作是由 Content Lead 逐项确认上述初筛并签发质量结果；确认后再在公司受控系统生成快照 SHA-256、`srcv_*` 和 ACL EVD。本仓只接收脱敏聚合结果，不接收话术正文或敏感命中值。
