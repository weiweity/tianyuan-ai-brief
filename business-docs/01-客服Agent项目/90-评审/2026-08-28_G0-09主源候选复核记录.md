# G0-09 主源候选复核记录

> **记录性质：** `READ-ONLY REVIEW` · 四域主源候选复核 · 不构成 `EVD-*`，不改变 G0-09 / Scope #9 状态，不授权 G0 / Ddev。
>
> **复核日期：** 2026-08-28 · **复核视角：** 当前 lark-cli `user` 身份

## 1. 本轮结论

用户已明确：当前公司空间中的客服工作簿副本将作为后续客服项目的主要维护入口。新鲜只读复核确认该工作簿包含“售前、售后、活动、产品话术”四个普通网格工作表。2026-08-28，用户声明持有 `ROLE-CONTENT-LEAD` 角色，并确认四域候选映射进入 `canonical/current` 复核，因此将候选状态从 `PRECONFIRM` 推进为 `IN_REVIEW`。

该确认只建立“进入复核”的程序效力，仍不等于 Content Lead 已将四个工作表分别签发为唯一 `canonical / current`，也不替代现有产品 / 活动来源。版本、快照、质量、权限和最终批准证据齐备前，不写入 G0-09 关闭收据，不增加通过计数。

## 2. 工作簿与权限新读取

| 检查项 | 新鲜结果 | 证据边界 |
| --- | --- | --- |
| 工作簿结构 | 4 个可见 `sheet`：售前、售后、活动、产品话术 | 证明四域结构存在，不证明四域主源已批准 |
| 当前 revision | `0` | 只能作为后续受控快照的起点，不是最终版本 EVD |
| 组织外访问 | `external_access_entity=closed` | 当前副本自身权限读取结果 |
| 组织内链接 | `link_share_entity=tenant_readable`、`share_entity=same_tenant` | 保留组织内只读协作，不等于匿名访问 |
| 复制 / 打印 / 下载 | `copy_entity=only_full_access`、`security_entity=only_full_access` | 当前副本自身权限读取结果 |
| 评论 / 协作者管理 | `comment_entity=anyone_can_view`、`manage_collaborator_entity=collaborator_can_view` | 按接口现值记录，不扩大为管理员专属 |
| 外部邀请 | 写入响应接受 `invite_external=false`，但权限读取接口未回显该字段 | 不作为独立闭环 EVD，仍需独立受控管理员证据 |
| 密级 | 页面新读取显示 `L1` | 副本密级已设置；不推断其他来源密级 |

本轮只读；未修改在线工作簿、未读取并保存正文、未修改原始来源。

## 3. 结构 / 敏感格式初筛

使用 `+table-get` 做一次性只读聚合扫描，未保存原文。敏感格式命中仅表示正则初筛命中，不判断是否为真实个人信息、业务热线或合法模板，也不等同于最终隔离结论。

| 工作表 | used range | 物理数据行 | 非空行 | 空行 | 非空整行重复副本 | 敏感格式命中 | 结构备注 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | --- |
| 售前 | `A1:C82` | 81 | 79 | 2 | 0 | 0 | 第三列为 `col3` 占位列，实际无值；旧摘要的“2 重复”是两条空行互相相同 |
| 售后 | `A1:C224` | 223 | 223 | 0 | 0 | 3 | 第三列为 `col3` 占位列，手机号格式初筛命中 3 行 |
| 活动 | `A1:B5` | 4 | 4 | 0 | 0 | 0 | 两列均有值 |
| 产品话术 | `A1:C107` | 106 | 106 | 0 | 0 | 0 | 首列表头为空白，但该列有值，需确认业务含义 |

合计为 414 条物理数据行、412 条非空行。`2026-08-28`，`ROLE-CONTENT-LEAD` 已确认售前域质量分母 `81 / 79 / 2`，确认售后域 3 条号码格式命中均为公司业务号码、质量分母为 `223 / 223 / 0`，并确认活动域质量分母 `4 / 4 / 0`、渠道 `千牛 / 抖音`、`2026-09-01～2026-09-30` 时间窗、到期未复核自动停用规则，以及商品范围 `storewide`（`product_scope_refs=[]`）且仅限当前冻结 4 条活动话术。产品话术空表头仍需由内容 Owner / Content Lead 判断；冲突、其他域适用范围、占位符和风险等级也尚未全部完成业务语义审查。

## 4. G0-09 解阻清单

1. Content Lead 已确认四域候选映射进入复核；仍需逐域完成唯一主源结论并签发 `canonical / current`。
2. 售前、售后、活动域质量分母已确认，活动渠道、时间窗与 `storewide + []` 商品范围已确认，且活动内容集合锁定为冻结 4 条；内容 Owner / Content Lead 继续复核产品话术空表头并给出产品域脱敏 `total_rows / importable_rows / quarantined_rows`。
3. 在公司受控系统冻结 revision `0` 对应快照并计算 SHA-256，生成新的 `SRC-* / srcv_*` 映射；快照与原始 hash 不进入本仓。
4. 独立受控管理员补齐四域现行 ACL、继承关系、角色组、机器人 / API 与限时例外证据；当前用户对原候选源的权限读取仍受 `permission_denied` 阻塞。
5. 四域分别形成来源 / 质量 / ACL EVD，并由同一 `EVD-G0-09-AUTHORITY-SOURCES-YYYYMMDD` 整体收据收口后，才运行 G0-09 / Scope #9 关闭校验。

在上述材料完成前，G0-09 继续 `进行中`，Scope #9 未勾选，G0 未签发，Ddev 未授权，产品功能代码不得开始。

## 5. 身份分层复核补记

2026-08-28 再次使用当前 `lark-cli` 身份做只读权限检查，确认以下结果：

| 身份 | 原始两个 MENOKIN 知识库文档 | 公司空间“客服agent”副本 | 结论 |
| --- | --- | --- | --- |
| 当前企业用户 | `permission_denied` | 可读取并可读取权限设置 | 用户协作权限与机器人权限不可混用 |
| 智能助手机器人 | `permission_denied` | 已加入 `view` 协作者；工作簿元数据与 revision 可读 | 机器人数据入口已部分验证；原始来源仍无权 |

本补记不等于申请或授予权限，也不改变在线文档设置；后续如需机器人 / API 读取，只能由独立受控管理员在公司空间内向指定副本授予最小只读权限，并形成可复核的主体、资源、角色、继承与限时例外证据。组织外访问仍应保持关闭。

## 6. 应用级读取复核补记

应用管理员发布 `sheets:spreadsheet:read` 后，机器人身份已成功读取工作簿元数据和 revision `0`，证明应用级读取 scope 已生效。随后对四个工作表各读取一次 `A1` 做最小数据入口验证：售前、活动、产品话术通过 `+csv-get` 成功；售后该原语连续两次遇到 TLS handshake timeout，改用只读 `+cells-get` 成功，因此四域最小读取入口均已验证。

当前状态拆分记录为：

- 文档级协作者授权：已写入，并由用户身份回读确认；
- 应用级 Sheets 读取 scope：已生效，工作簿结构 / revision 可读；
- 最小内容读取：`4/4` 工作表成功；售后通过替代只读原语完成验证；
- 公共访问设置：未改变，组织外访问仍为关闭。

售后已通过替代只读原语完成最小读取；后续若需要统一 CSV 读取格式可在网络稳定后补做，但不影响本轮“机器人可进入四域数据入口”的结论。仍需由 Content Lead 完成四域主源、质量分母、冻结快照和 ACL EVD；这些完成前不能形成 API / RAGFlow 读取链的完整证据，也不能关闭 G0-09。

## 7. 本地接收预检与合同校验

使用仓库现有 `verify_customer_agent_g009_intake.mjs` 对当前模板执行默认预检，结果为 `INCOMPLETE`；四个域均未达到 `READY_FOR_G0_UPDATE`，并明确声明不授权 `G0-09`、`Scope#9`、G0 签发、Ddev 或代码开发。

本轮静态校验结果：G0-09 接收合同测试 `6/6` 通过，来源工作包相关 Python 测试 `9/9` 通过，项目状态回归测试 `22/22` 通过，公开边界测试 `6/6` 通过，`git diff --check` 通过。上述均为仓库合同 / 静态边界证据，不替代 Content Lead 的业务签发、受控快照或 ACL EVD。

## 8. Content Lead 复核入口与质量口径补记

2026-08-28 本轮已登记 `ROLE-CONTENT-LEAD` 的“进入复核”确认。随后使用当前用户身份对唯一标题候选做逐表只读聚合，四表均成功、未输出原始单元格值、未执行在线写入。

本次将“空行”与“非空整行重复”分开计数，确认四域非空整行重复副本均为 0。`ROLE-CONTENT-LEAD` 已明确确认售前 `81 / 79 / 2`、售后 `223 / 223 / 0` 与活动候选 `4 / 4 / 0`，并补充确认活动渠道、时间窗、`storewide + []` 商品范围和“仅限冻结 4 条话术”的内容边界。售前、售后数字已回填受控 `closure_manifest.json`；活动行仍绑定现行 8 月来源版本，不能混入新候选分母，因此活动决定只登记在候选复核记录，待新候选完成独立 `source_ref / source_version_id` 受控登记后再写入关闭清单。各域仍缺不同组合的主源标识、快照、ACL、适用范围、`quality_evd` 和整体批准收据，因此仍为 `INCOMPLETE`。活动域适用范围裁决已完成；产品话术空表头继续等待业务语义裁决。

## 9. 活动域适用范围只读补记

2026-08-28 对公司空间唯一标题候选的“活动”工作表再次做最小只读检查：实际范围为 `A1:B5`，共 4 条数据、2 列，读取未截断。扫描未发现渠道字段、完整日期、月日、日期区间、生效 / 失效 / 复核标签或“长期有效”等标记，因此不能从这 4 条内容本身推出有效期。

产品契约已将一期渠道限定为 `千牛 / 抖音`；当前活动主源 `SRC-04A9A86874258A6A / srcv_76b9165b2fe31908` 已登记为 `2026-08-01` 至 `2026-08-31` 有效，且其 `67 / 66 / 1` 分母与新候选 `4 / 4 / 0` 明显不是同一冻结版本。`ROLE-CONTENT-LEAD` 已于 2026-08-28 明确确认新候选适用于 `千牛 / 抖音`，于 `2026-09-01` 生效，`2026-09-30` 复核，到期未复核自动停用，并确认商品范围为 `storewide`、`product_scope_refs=[]`，仅适用于当前冻结的 4 条活动话术。这里的空 refs 只因显式 `storewide` 类型而合法，不是空范围旁路；新增或改写话术必须形成新版本并重新批准。该决定已完成活动域适用范围裁决，但受控映射、快照和批准证据完成前，当前仍不改写现行活动主源。

同日 fresh read 再次确认活动表为 `A1:B5`、4 条、2 列且未截断；当前用户身份读取到组织外访问关闭、组织内链接可读、复制 / 下载仅完整权限用户可用。本次权限读取仅覆盖目标自身设置，不覆盖父级继承、直接协作者 / 角色组、历史权限变更或审计日志，因此只计部分 ACL 事实，不签发 ACL EVD。受控工作包已生成独立随机候选 `SRC-* / srcv_*` 与内容指纹草案，状态为 `DRAFT_NOT_EVIDENCE`；原始指纹不进入公开台账或 Git。
