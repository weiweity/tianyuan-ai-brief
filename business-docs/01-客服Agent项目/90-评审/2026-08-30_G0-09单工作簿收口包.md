# G0-09 单工作簿收口包

> **内容快照 / 质量证据：** `EVD-G0-09-WORKBOOK-CLOSURE-20260830`
>
> **整体批准证据：** `EVD-G0-09-AUTHORITY-SOURCES-20260830`
>
> **ACL 证据：** `EVD-G0-09-ACL-OWNER-BASELINE-20260830`
>
> **状态：** `SIGNED · L1_SINGLE_OWNER · CANONICAL_CURRENT`
>
> **决定依据：** `DEC-053 / DEC-057 / DEC-058`

## 1. 一次只读回读

2026-08-30 23:15（Asia/Shanghai），使用已验证的公司用户身份，通过飞书官方接口按精确标题唯一定位公司内部电子表格，并只读获取工作簿结构、当前版本和四个子表的类型化数据。处理过程只在内存中计算脱敏聚合与内容摘要；公开仓不保存原始摘要、URL、token、成员 ID、手机号或话术正文。

公开安全映射：

| 项 | 公开值 |
|---|---|
| 工作簿来源代号 | `SRC-92847D5B505F17C4` |
| 物理工作簿版本代号 | `WBV-531DD7AA4BA2B97B` |
| 内容摘要 | 不进入公开仓；公开一致性仅使用上述 `WBV-*` 与各域 `srcv_*` 不透明代号 |
| 脱敏快照 | [`2026-08-30_G0-09单工作簿脱敏快照.json`](2026-08-30_G0-09单工作簿脱敏快照.json) |

`SRC-* / WBV-* / srcv_*` 是本次受控取证生成的公开不透明代号：`WBV-*` 只标识物理工作簿版本，四个 `srcv_*` 分别标识可进入 CR-004 注册表的逻辑域版本。资源映射、生成输入与真实 revision 只保存在公司受控系统。版本变化时必须生成新的 `WBV-*` 与四个 `srcv_*`，旧值不得继续作为 `current`。

## 2. 四个逻辑域共用一个物理版本

| 域 | 逻辑版本 ID | 子表结构事实 | 总数 / 可导入 / 隔离 | 结论 |
|---|---|---|---:|---|
| `presale` | `srcv_52af2c0a648a7f8c` | “售前”；业务列为“快捷短语 / 产品话术” | `81 / 79 / 2` | 2 行两个业务字段均为空，隔离；其余可导入 |
| `campaign` | `srcv_2eb1831b70eddfbc` | “活动”；业务列为“快捷短语 / 产品话术” | `4 / 4 / 0` | 千牛 / 抖音；`storewide`；仅冻结 4 条；有效至 2026-09-30 |
| `aftersale` | `srcv_8e163328604d0765` | “售后”；业务列为“快捷短语 / 产品话术” | `223 / 223 / 0` | 全部业务字段齐全 |
| `product` | `srcv_c5d5b8e6a761893d` | “产品话术”；首列为“适用产品” | `106 / 106 / 0` | “适用产品 / 快捷短语 / 产品话术”全部齐全 |

四域共享同一 `source_ref`、物理 `WBV-*`、`snapshot_evd` 和 `quality_evd`，但每个域拥有独立 `source_version_id` 与逻辑 binding，以满足现行 CR-004 注册表的主键和 domain 约束。不得把“一个工作簿”误写成“一个内容域”或“一个逻辑版本”。

## 3. ACL 与最终批准

- ACL 继续由 [`2026-08-30_G0-09单人Owner权限基线.md`](2026-08-30_G0-09单人Owner权限基线.md) 覆盖；
- Content Lead 已确认四个子表分别作为售前、活动、售后、产品的 `canonical / current`；
- 本次一次性批准 `SRC-92847D5B505F17C4@WBV-531DD7AA4BA2B97B` 及上述四个逻辑版本的映射、质量分母、活动有效期和冲突规则；
- 任何内容、所有权、成员、分享范围、应用权限或 revision 变化都使当前版本失效，必须生成新 `WBV-*` 与四个 `srcv_*` 并重新收口；
- 其他同域文档只能登记为 `reference / superseded`，不得与当前版本混用。

因此 G0-09 与 Scope #9 使用同一个整体批准证据 `EVD-G0-09-AUTHORITY-SOURCES-20260830` 关闭。

## 4. 边界

- 本包只冻结开发前的权威内容身份与质量摘要，不把真实话术写入产品仓；
- 不证明正式飞书 OAuth / App RBAC、内容导入、数据库发布或 RAGFlow 索引已实现；
- 真实数据、多人协作、飞书运行接入、RAGFlow 真实索引、部署或真实 Pilot 前仍须通过 `PROD-ACL-01`；
- 本包不签发 G0、Ddev、Git、部署或发布授权。
