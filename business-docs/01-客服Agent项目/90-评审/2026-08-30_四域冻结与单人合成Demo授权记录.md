# 2026-08-30 · 四域冻结与单人合成 Demo 授权记录

> **状态：** `DECISION_RECORD · USER_AUTHORIZED`；本文件的 `INCOMPLETE` 描述是冻结当时快照，G0-09 / Scope #9 后由 `DEC-058` 与单工作簿收口包关闭
> **生效：** 四域业务冻结自记录时生效；单人纯合成 `DEMO-M0` 自 2026-08-31 生效
> **复核：** 2026-09-30
> **不构成：** G0-09 `EVD-*`、Scope #9 Pass、G0 签发、正式 `DEC-DDEV-01`、真实数据或部署授权

## 1. 四域冻结决定

| 域 | 唯一业务候选 | 冻结质量口径 | 适用 / 有效期 |
|---|---|---|---|
| 售前 | 企业工作簿“售前”子表；其他同域材料只可为 `reference / superseded` | `81 / 79 / 2` | 受控版本和 EVD 齐备后切换 `canonical / current` |
| 活动 | 企业工作簿“活动”子表；仅当前冻结 4 条 | `4 / 4 / 0` | 千牛 / 抖音；`storewide + []`；2026-09-01 至 2026-09-30，未复核则 2026-10-01 自动停用 |
| 售后 | 企业工作簿“售后”子表；其他同域材料只可为 `reference / superseded` | `223 / 223 / 0` | 三条号码格式命中均为公司业务号码，不在 Git 或公开材料披露号码 |
| 产品 | 企业工作簿“产品话术”子表；首列正式字段名为“适用产品” | `106 / 106 / 0` | A / B / C 三列 106 行均非空；3 个适用产品；整行重复副本 0 |

所有域的后续新增、删除、改写、适用范围或有效期变更都必须创建新版本，禁止覆盖本次冻结口径。活动机器合同使用半开区间 `effective_from=2026-09-01`、`effective_to=2026-10-01`，从而完整覆盖 2026-09-30。

本次只读复核使用企业用户身份，在线数据只用于结构和聚合判断；未把话术正文、飞书 URL / token、真实号码、成员清单或原始快照写入 Git。

## 2. 仍需补齐的正式证据

- 四域公开安全 `source_ref / source_version_id` 与受控私有映射；
- 不可变导出快照、原始 SHA-256 与 `snapshot_evd`；
- 当前 ACL、父级继承、协作者 / 角色组、外链、管理员与审计证据；
- 四域逐域 `quality_evd`；
- `ROLE-CONTENT-LEAD` 四域整体批准 `EVD-G0-09-AUTHORITY-SOURCES-YYYYMMDD`。

上述证据未齐前，四域关闭收据继续为 `INCOMPLETE`，不得把本记录改写为 `READY` 或正式 G0 签发。

## 3. 单人纯合成 DEMO-M0 授权

| 项 | 决定 |
|---|---|
| 产品仓 | `customer-agent-prototype` |
| 人力 | 单人 FDE；同一时间一个纵向切片 |
| 生效 / 复核 | 2026-08-31 / 2026-09-30 |
| 费用 | 新增付费 0；不采购外部模型、云服务或工具 |
| 允许 | 纯合成 fixture、Fox / Query / Dashboard UI 与交互、状态模型、测试、构建、开发诊断、不计正式通过的 Windows feasibility smoke |
| 禁止 | 真实客户或飞书内容、凭证、正式 API / worker / migration、PostgreSQL 运行接入、网络出域、遥测采集、付费调用、自动发送、部署、Pilot、生产承诺 |
| Git | 本记录不授权 commit、push、PR、merge 或 deploy；继续逐项授权 |

该旁路是为单人 Demo 降低流程成本的可逆决定。它不会把项目台账中的“产品开发=未开始”改成正式开发中，也不会让 Demo 测试结果反向关闭 G0 / Ddev。

## 4. 对应真源

- `DEC-053`：四域唯一候选、质量分母、产品字段与活动有效期；
- `DEC-054`：单人纯合成 `DEMO-M0`、费用 0、2026-09-30 复核；
- 当前状态与门禁仍以 `00–06` 真源和 `02` 的四域关闭收据为准。
