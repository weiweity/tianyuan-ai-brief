# 客服 Agent · DEV-M0-W1 机械迁移完成记录

> **项目：** `CS-AI-C11` · 客服 Agent 一期
> **证据 ID：** `EVD-DEV-M0-W1-20260831`
> **证据状态：** `VERIFIED · MERGED`
> **组织门输入：** `DEC-DDEV-01=PASS` / `EVD-DDEV-AUTH-20260831`
> **产品仓切片输入：** 用户已分别授权 W1 开工、提交、推送、PR、Review 修复、合并重试与候选分支删除
> **取证日期：** 2026-08-31

## 1. 结论

产品实施仓 `customer-agent-prototype` 的 `DEV-M0-W1 · desktop mechanical move` 已完成，并通过 PR #9 合并到 `main`。合并头为 `6c759c6b317d9382787dcec200f3e828b7a5007d`，产品仓当前仅保留 `main`。

该结论只证明桌面应用机械迁移和行为等价验证已落库，**不表示 `DEV-M0`、技术第 4 关或技术第 5 关已完成**。合同开发、codegen / runtime validation 与后续能力仍须单独授权。

## 2. 产品仓实施事实

| 项 | 取证值 |
| --- | --- |
| W0 合并基线 | `6111272faf1adf9b2e457f9d6458c884480d06f6` |
| W1 Review 修复头 | `1f10495` |
| 合并 PR | `#9` |
| 合并后 `main` | `6c759c6b317d9382787dcec200f3e828b7a5007d` |
| 实施记录 | `docs/plans/2026-08-31-dev-m0-execution.md` |
| W1 主要产出 | 根 Electron 应用及其源码、测试、资产、构建与打包配置机械迁入 `apps/desktop/`；根命令保持稳定 |
| 行为与信任边界 | 未新增 API、worker、DB、OAuth、遥测、真实数据、UI 行为或 IPC 能力；renderer 安全边界不扩张 |
| 合同激活 | 保持 `ddev_authorized=false` / `runtime_activated=false` |

## 3. 已实际运行的验证

运行环境：Node.js `v24.19.0`、pnpm `11.19.0`。

| 命令 / 检查面 | 结果 |
| --- | --- |
| `pnpm install --offline` | PASS · 2 个 workspace project，0 下载 |
| `pnpm lint` | PASS · 0 warning |
| `pnpm typecheck` | PASS |
| `pnpm test` | PASS · 51 files / 506 tests |
| `pnpm build` | PASS |
| `pnpm workspace:check` | PASS · root policy PASS，7.20 MiB / 32.0 MiB |
| `pnpm contracts:verify` | PASS · 合同集、来源 SHA 与双哈希通过，仍未激活 |
| `pnpm test:e2e` | PASS · 14/14，迁移后 Electron 窗口、IPC、复制与 Dashboard 路径保持等价 |

## 4. 未放行边界

- W1 未生成 DMG、ZIP 或 Windows 安装包；未运行签名、公证、Windows 真机、部署、生产或 UAT。
- 真实数据、飞书运行接入、RAGFlow 真实索引、凭证、网络出域、遥测、付费调用、自动发送、部署与 Pilot 继续 NO-GO。
- W1 回归测试是机械迁移的实现证据，不表示技术第 5 关“单元测试”已经进入或完成。
- 下一动作仅为“合同开发授权与 codegen / runtime validation”的授权准备；授权形成前不得实施，也不得擅自命名或启动新的 W* 切片。

## 5. 当前后续动作

治理仓将当前状态更新为：`DEV-M0 · IN_PROGRESS`，`W0`、`W1` 已完成；下一能力待单独授权。`DEV-M0` 退出证据未齐前不得进入 `DEV-M1`。
