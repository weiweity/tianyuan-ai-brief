# 客服 Agent · DEV-M0 开工与 W0 基线验证记录

> **项目：** `CS-AI-C11` · 客服 Agent 一期
> **证据 ID：** `EVD-DEV-M0-W0-20260831`
> **证据状态：** `VERIFIED · WORKTREE_ONLY · NOT COMMITTED`
> **组织门输入：** `DEC-DDEV-01=PASS` / `EVD-DDEV-AUTH-20260831`
> **产品仓开工输入：** 用户已明确授权“开工授权 / DEV-M0 产品仓开工授权”
> **取证日期：** 2026-08-31

## 1. 结论

产品实施仓 `customer-agent-prototype` 已进入 `DEV-M0 · IN_PROGRESS`。首个切片 `DEV-M0-W0 · pre-move baseline + workspace scaffold` 已完成；下一切片是 `W1` mechanical move。

该结论只证明代码开发已开始且 W0 完成，**不表示 `DEV-M0` 或技术第 4 关已完成**，也不放行 `DEV-M1`。

## 2. 产品仓实施事实

| 项 | 取证值 |
| --- | --- |
| 分支 | `codex/dev-m0-workspace-scaffold` |
| 取证时 HEAD | `26dcdd1f4f683a88b589c949540b3ee583017ea6` |
| 实施记录 | `docs/plans/2026-08-31-dev-m0-execution.md` |
| 当前运行入口 | 仍为根 Electron 应用；未建立第二套入口 |
| W0 主要产出 | 根 workspace 策略、`apps/desktop` 目标位、工具链锁定、workspace hygiene 失败关闭校验、迁移前 E2E 基线 |
| 合同激活 | 保持 `ddev_authorized=false` / `runtime_activated=false`；组织开工授权不冒充运行时激活 |
| Git 状态 | 变更仍在工作树；本记录不表示已 stage、commit、push、PR 或 merge |

## 3. 已实际运行的验证

运行环境：Node.js `v24.19.0`、pnpm `11.19.0`。

| 命令 / 检查面 | 结果 |
| --- | --- |
| `pnpm lint` | PASS |
| `pnpm typecheck` | PASS |
| `pnpm test` | PASS · 50 files / 495 tests |
| `pnpm build` | PASS |
| `pnpm workspace:check` | PASS · 7.16 MiB / 32.0 MiB，`release/` 单独分账 |
| `pnpm contracts:verify` | PASS · 精确合同集、来源 SHA 与双哈希通过 |
| 聚焦 E2E 重复回归 | PASS · 10/10 |
| `pnpm test:e2e` | PASS · 14/14 |
| `pnpm package:mac:local` | PASS · universal 未签名 DMG / ZIP，未公证、未发布 |

## 4. 未放行边界

- 未运行 Windows 真机、签名 / 公证、生产、真实坐席或 UAT 验证。
- 真实数据、飞书运行接入、RAGFlow 真实索引、凭证、网络出域、遥测、付费调用、自动发送、部署与 Pilot 继续 NO-GO。
- `W0` 不包含 API / worker / DB 空包、正式 DTO / migration 或合同 runtime 激活。
- 任何 commit、push、PR、merge、deploy 和分支删除仍须分别授权。

## 5. 下一切片

`DEV-M0-W1` 只做机械迁移：把当前根 Electron 应用原样迁入 `apps/desktop`，修正导入、脚本与测试配置，并用同一质量门证明行为等价。完成该结构变更后，才进入合同开发授权、codegen / runtime validation 与后续 `DEV-M0` 切片。
