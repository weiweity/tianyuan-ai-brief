# 客服 Agent · Ddev 正式签发记录

> **项目：** `CS-AI-C11` · 客服 Agent 一期
> **决策：** `DEC-DDEV-01`
> **授权证据：** `EVD-DDEV-AUTH-20260831`
> **签发时间：** 2026-08-31 01:44:57 +0800
> **签发 Owner：** `USR-TIANYUAN-001`
> **结论：** `PASS` · `DDEV AUTHORIZED / DEV-M0 ONLY`
> **授权投影 SHA-256：** `25f5ef3f7c63b3bbe2c5d9f38de2432b6139892792b68ce0e214a8b221874a2e`

## 1. 签发依据

- G0-02～15：Pass 14 / 14；Scope：Pass 15 / 15；G0 证据 `EVD-G0-SIGN-20260831`。
- 七类 Owner 从现行 RACI 唯一投影，项目负责人及代理、职责接受与证据均已满足签发门。
- 费用采用 B 路径：新增付费 0，下次决策日 2026-09-30。
- 冻结输入：`01` 排期 v3.28、`03` Scope v4.43、`04` 费用 v3.13、`37` 架构 v1.16、`46` 实现设计 v1.22。

## 2. 授权结果

- 授权总边界采用 A · 条件式授权，覆盖已冻结的一期 `DEV-M0～DEV-M4` 开发包。
- 本次只即时放行 `DEV-M0`；未形成 `DEV-M0` 退出证据前不得进入 `DEV-M1`。
- 允许环境为 `development / test`，允许数据仅为合成数据。
- Ddev 自 2026-08-31 生效，2026-09-30 复核。
- 本记录只改变组织授权状态；产品仓代码不会因签发自动开始，实际开工须在后续独立任务中形成实现与验证证据。

## 3. 继续禁止

本次不授权：真实或未经单独批准的脱敏数据、飞书运行接入、RAGFlow 真实索引或集成、真实坐席试点、生产环境与部署、付费调用、遥测采集、自动代发、教师调用、训练或模型发布。

本次也不包含任何仓库的 stage、commit、push、PR、merge、deploy 或分支删除授权；这些动作仍须分别取得明确授权。

## 4. 失效与后续门

Scope、Owner、安全、费用、数据边界或关键架构发生变化时，立即暂停新增开发，先登记 CR / DEC 并重新签发。`DEC-AUTH / SEARCH / CONTENT / UPLOAD / CAP / DATA / METRICS / WORKORDER / ITERATION-TASK / EGRESS / OPS / PUBLISH` 继续按实现设计 §13.2 到期生效，Ddev 不替代任何后续能力门或 `DEC-PUBLISH-01`。

台账中的开发进度和排期状态可独立前进，但不得修改本次 Ddev 决策表、Scope、费用、架构或实现设计授权投影；任一受控投影变化都会使机器校验失败并要求重新签发。

---

*本记录是公开安全投影，只保存人员代号、决定与 EVD ID；真实姓名、确认原文和内部链接保存在公司受控系统。*
