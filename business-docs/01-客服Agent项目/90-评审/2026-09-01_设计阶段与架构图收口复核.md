# 客服 Agent · 设计阶段与架构图收口复核

> **复核日期：** 2026-09-01
> **项目：** `CS-AI-C11`
> **结论：** `PASS · DESIGN CLOSED / DEV-M0 IN PROGRESS`
> **证据类型：** 文档发布与目录一致性复核；不替代运行时、测试、Pilot、部署或生产证据

## 1. 阶段结论

- 技术第 1 关需求分析：`Pass · 已收口`。
- 技术第 2 关架构设计：`PASS-WITH-CONDITIONS · 静态设计已冻结`。
- 技术第 3 关实现设计：`Pass · 文档包 Ready · 已收口`。
- 组织授权门：G0 / Ddev 已分别签发；当前只放行 `DEV-M0`。
- 技术第 4 关代码开发：`DEV-M0 · IN_PROGRESS`；产品仓 W0、W1 已完成，下一能力仍待单独授权。

## 2. 目录处理

`20-设计-进行中/` 保留为 Ddev 签发时冻结的合同兼容路径。Ddev 授权投影包含 `03` Scope、`04` 费用、`37` 架构与 `46` 实现设计的完整内容；其中存在冻结路径与合同集来源引用。直接改名并修正文档会改变授权投影，只移动文件又会制造失效链接和合同路径漂移。本次因此只收口状态和入口，不改签名兼容路径，也不重新签发 Ddev。

`30-开发-待开始/` 不属于签名输入，且当前产品仓已进入 DEV-M0，因此改名为 `30-开发-进行中/`，同步修正文档地图、分类汇总和项目导航。

## 3. 架构图复核

- 图源：8 份 `.puml`。
- 独立派生图：8 份 `.svg`。
- 浏览器图板：8 个 Tab；首图为一期开发框架。
- 瀑布图当前投影：需求、架构、实现设计已通过；代码开发为 `DEV-M0 IN_PROGRESS · W0 / W1 COMPLETE`；第 5～8 关未开始。
- 校验命令：在 Node.js 24.x 环境运行 `npm --prefix sites run check:arch-diagrams`。
- 校验结果：`PASS PlantUML source/SVG/HTML aligned (8/8)`。

架构图表达与项目当前状态一致。它仍明确区分静态设计冻结、DEV-M0 开发、运行时验证、系统测试、发布与生产，不把 W0/W1 回归证据冒充技术第 5 关或上线证据。

## 4. 重复与多余文档审计

- 本次收口后客服项目共 76 份 Markdown；按 SHA-256 未发现完全相同的重复文件。
- 一级标题未发现重复，除各目录约定的 `README.md` 外未发现重复文件名干。
- `90-评审/` 保存带日期证据，`99-历史/` 保存被替代设计和旧治理方案；内容有时间语义差异，不应合并或删除。
- 当前 `20` 目录只保留现行设计合同、图源和对应测试；已替代的 08-06 初始设计包已经位于 `99-历史/2026-08-06-架构设计收口/`。

因此本轮没有删除历史证据或设计合同。删除这些文件不会提升真源清晰度，反而会破坏审计链和既有引用。

## 5. 未扩大范围

本复核不授权合同开发、codegen、runtime、真实数据、飞书运行接入、RAGFlow 真实索引、遥测、Pilot、部署、付费调用、自动发送、commit、push、PR、merge 或分支删除。
