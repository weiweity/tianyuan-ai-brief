# 四域业务批准确认与工程准入差异

日期：2026-09-05。类型：用户会话事实澄清与本地工程核对；不签发新阶段门。

## 1. 已确认事实

用户明确说明：售前、售后、活动、产品话术四域表格均从业务取得，均为审核通过版本，并要求继续已批准路径直至收口。现有业务原文不重写、不删减，不启动另一轮全量内容审批。

产品资料、售后规则、活动规则暂停扩展。AI 选句、多模型评测、蒸馏、RAGFlow 保留为暂停备忘，不作为软件上线前置条件。

## 2. 澄清历史计数

现行台账和产品 G1a 计划中的“正式逐条审批 0/33”是装包风险签收文件齐备计数，不是四域业务版本批准计数。当前输入包的 33 条检索项与四域源表总体不是同一分母。

后续使用两个独立表述：业务四域版本已由用户确认通过；逐条风险元数据及合同签收记录未齐。用户的业务确认不伪装成另一个真实主体签字，也不据此自动批准尚未确认的模型风险分类。

## 3. 尚需解决的工程衔接

| 项目 | 核验结果 | 处理边界 |
| --- | --- | --- |
| 原包失败 | medium 与非空风险类别组合违反合同 | 修复字段映射，不按域硬编码、不清空类别求通过 |
| 逐条输入 | confirmed 文件不存在；待审文件有 33 项，primary/secondary 均未填写 | 核对既有业务审核如何承接风险元数据和身份记录，不重审业务原文 |
| 不同主体审核 | 装包 validator 和冻结数据库合同要求 high/冲突由不同主体审核 | 不能代造第二主体；若业务现行流程不同，先明确治理合同调整决定 |
| 版本化入口 | 旧装包入口仍绑定历史产品基线及原输出路径 | 新包须独立输出，不覆盖旧包；完成批准承接后再装配 |
| 运行与上线 | T5 没有有效报告，T6 未签，桌面未接真实搜索 | 本澄清不签发 T6、不放行 DEV-M2、部署或自动发送 |

**后续决定已收到（2026-09-05）：** 用户在“既有两人审核，还是调整为由你统一确认承接”的明确问题后回复“自己承接吧”。据此记录为用户/项目负责人统一承接这批四域已审核业务版本的内容批准责任；不再等待其重复选择或提供另一审核人。该决定只覆盖本批业务版本的批准承接，不代表用户替所有后续版本、QA、数据安全、部署和运行门统一签发。

**实现状态：决定已确认，合同转换未实施。** 后续应在治理合同中显式表达负责人承接的受限适用范围、版本绑定及留痕，再同步产品快照、新增 migration、输入校验、仓外装包和回归。不得直接修改历史 migration、伪造 secondary reviewer、清空风险标签，或放行未知/过期版本。风险元数据按规则完成工程映射，内容不重写；未决内容冲突仍应可见，不用本决定自动清除。

原表中“先明确治理合同调整决定”的等待项由上述用户决定结束，但机器约束仍然存在；旧证据保留当时语境，不把当前决定倒签进历史双审文件。

## 4. 保存与后续

原有证据、表格、金标和失败记录全部保留。既有包有效期不自动延长，运行前必须重新校验。此文件仅含脱敏事实，无真实话术、账号、内部 URL 或审批原文。

完成批准承接与版本化输入后，回到原顺序：离线评测、结果复核与签收、正式桌面接线、目标设备验收和上线准备。每阶段仍须实际证据，不把分支清理当成产品上线。

## 5. 搜索上线主线剩余工作（6 个板块，非精确 issue 数）

| 顺序 | 板块 | 当前缺口与收口产物 |
| --- | --- | --- |
| 1 | 负责人承接合同落地 | 用户决定已确认；核对治理人读/机器合同、产品快照与新增 migration、输入验证、装包器的变更面；完成受限承接模式与失败关闭回归，旧版本/未知批准不得借此放行 |
| 2 | 真实话术版本化入包 | 绑定已审核四域版本，完成风险字段和有效期映射，保留原文与历史包；新包/外部锚点/权限/DLP/金标一致性通过，过期不自动续期 |
| 3 | 真实搜索质量验收 | 按有效授权与最终 dry-run 执行离线评测，验证召回、禁返、范围/过期/无结果及清理；产真实报告后复核签收，当前 T5 无报告、T6 未签 |
| 4 | 正式内容与身份运行链 | 现有为 synthetic/mock auth，正式飞书身份/访问控制、内容导入发布与回滚、readiness、PROD-ACL-01 和最小部署配置仍须实现及核验；不把已有 API 骨架当作生产可用 |
| 5 | 正式桌面搜索闭环 | 后续 DEV-M2 授权后，Main 窄 adapter 对接搜索，完成平台/商品上下文、来源版本、错误/无结果、人工复制；正式模式不得悄悄回退合成话术，合成仅留测试 profile |
| 6 | 内部灰度与上线交付 | 目标 Windows 设备联调、权限/性能、安装签名更新、日志脱敏、监控、备份恢复及回退；按已有范围先 3–5 内部坐席灰度并验收，再批准正式发布；不预报上线日期 |

每板块的测试、必要修复、提交/推送/PR/合并/分支清理是该板块交付步骤，不另算几十个功能任务；对外 Git 写入和实际运行/发布仍按明确授权执行。本表是优先级与缺口盘点，不代替各阶段的批准计划，也不宣布所有后续功能永久取消。

不纳入本次搜索上线主线：AI 选句、教师调用、多模型出题、蒸馏、RAGFlow、业务规则扩展。现有 4 项 S0/P3 测试待办仅在实际接线影响相关路径时纳入，不因待办存在就重开全量验证。工单分析和 Dashboard 扩展保留后续范围，不抢占搜索上线顺序。

## 6. 四域主源显示内容核对（追加，2026-09-05）

用户确认继续使用既有四域汇总工作簿作为项目主源，保留产品话术首列表头补齐；业务原表作为来源保留，不覆盖、不重新复制，不重开业务内容审批。具体内部链接仅在受控飞书侧保留，不进入 Git。

本次以用户身份只读比较业务原表与项目主源，包含隐藏行列；按实际返回范围及行列映射核对，未出现分页截断。下表行数是网格覆盖范围，不是有效话术数量，也不替换历史质量分母。

| 域 | 比较范围 | 单元格显示值结论 |
| --- | --- | --- |
| 售前 | A1:T187 | 一致 |
| 售后 | A1:T233 | 一致 |
| 活动 | A1:S200 | 一致 |
| 产品话术 | A1:T200 | 仅 A1 不同：原表为单个空格，主源为“适用产品”；其余一致 |

核对仅证明本次读取时的显示内容关系，不证明原始数值类型、公式、格式、批注、权限或后续版本持续一致，也不是可装包版本的 manifest 锚点。未修改在线表格、下载正文或执行真实评测；暂时性读取失败已通过后续成功读取补齐，不据此推断权限故障。

本项不再作为重复业务确认的阻塞。下一工程动作仍是负责人承接合同落地与版本化新包准备；T4/T5/T6 和运行、上线门状态不因本核对而改变。飞书项目文档同步与双仓 Git 交付另行核实，不把话术表一致写成全部文档已同步。

## 7. 外包团队访问范围（追加，2026-09-05）

用户确认外包团队仅服务本企业，不承载其他公司的话术。采用单企业租户下的独立外包账号与团队授权方向，不因使用地点在公司外就升级为多租户平台。本节记录已确认需求，不表示权限模型已实现、外部账号已开通或远程运行获准。

- 存储分工保持不变：Git 保存项目文档版本，飞书保存精选项目文档与业务话术主源；经批准、校验及版本化发布的话术进入 PostgreSQL，供后端检索。项目进度与审批文档不因 AI 搜索而整体导入产品数据库。
- 外包账号独立登录、不共用员工账号；服务端按明确授权的品牌、团队和内容版本校验搜索访问。不得依赖客户端自报团队或租户，不允许客户端直连数据库；未知范围默认拒绝。
- 默认不允许外包账号编辑、发布或批量导出话术，不默认授予飞书原表、项目文档及内部后台权限。人工复制已授权候选与批量导出分别控制，不能宣称客户端可读内容绝对无法被复制。
- 正式内容与身份运行链的验收新增外包账号配置、越权搜索与对象访问负例、团队授权变更、合作结束后的账号停用和会话撤销。若存在缓存或离线租约，必须明确撤权生效边界并验证，不能假定服务端停用即清除所有离线副本。
- 目标设备与发布验收新增公司外网络下的受控接入、传输保护、错误反馈及日志脱敏；部署方式、外包身份接入方式和撤权时限在后续实施计划中明确，本次不自动开放公网、发放账号、启用真实飞书运行链路或部署。

上述要求纳入第 5 节板块 4～6 的后续计划与验收，不改变当前 T4/T5/T6 状态，也不以文档留存替代外部坐席实测。未来若承载独立公司业务，再单独评估多租户合同与数据隔离。

## 8. 飞书项目文档留存准备（追加，2026-09-05）

已按用户确认，在当前企业用户根目录创建独立项目文档文件夹；元数据回读确认所有者为当前用户，直接子项为空且无后续分页。内部链接与用户标识不写入本仓，业务主源和历史证据均未移动或覆盖。

文件夹自身权限回读：链接分享关闭，组织外访问允许，已有阅读协作者可管理协作者。此为当前设置，不代表已发生外部访问或泄露。上传暂缓，计划仅收紧该文件夹的组织外访问和协作者管理范围，保持链接分享关闭；不修改成员或历史资料。

当前 CLI 的权限更新 schema 不接受 folder 类型，不能将文件夹伪装为 file 调用。后续需经用户确认通过飞书界面处理并回读验证，再展示精选上传清单。当前仅文件夹创建完成，项目文档上传、治理仓公开交付均未完成；本节追加使此前提交候选摘要失效，正式提交前须重新生成并按现行公开发布规则确认。

**后续执行追加：** 用户已授权通过界面收紧上述空文件夹，并建立本轮文档同步与双仓交付目标。界面操作后，CLI fresh read 确认 `external_access_entity=closed`、`link_share_entity=closed`、`manage_collaborator_entity=collaborator_full_access`。成员未作修改，复制下载和评论设置保持原值。该结果仅证明文件夹当前自身设置，不代替未来子文档权限回读、完整继承链或审计历史；首批文档上传尚未执行。
